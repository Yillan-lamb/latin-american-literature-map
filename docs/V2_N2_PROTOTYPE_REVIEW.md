# V2-N2 核心交互原型审核包

## 1. 节点信息

- 节点：`V2-N2`
- 交付任务：`V2-S4-005`
- 日期：2026-08-11
- 内部结论：`PASS`
- 当前状态：`👤 USER_REVIEW`
- 范围：真实 V1 数据驱动的核心交互原型；不进入 N2 之后的完整站点开发

## 2. 本次交付

网站入口：`site/index.html`

- 首页：地图主入口、N2 精选作者、N2 精选作品、简版时间线；
- 地图：国家层、国家→地点下钻、现实地点筛选、虚构空间筛选；
- 国家页：巴西、墨西哥、哥伦比亚；
- 地点页：里约热内卢、恰帕斯、科米坦、阿卡塔卡；
- 虚构空间页：科马拉、马孔多；
- 作者页：李斯佩克朵、鲁尔福、卡斯特利亚诺斯、马尔克斯；
- 作品页：6 部 N2 样本作品；
- 搜索：中文名、原文名、内容卡标题；
- 时间线：28 个作品文学节点、5 个历史背景节点，状态随 Web Data 保留；
- 双层阅读：普通阅读层 + 研究层折叠面板；
- 数据源：`data/v2/web/site_data.json`，前端不直接查询 SQLite，不写研究事实常量。

## 3. 自动化 QA

| 检查 | 结果 |
|---|---|
| V1 主库完整性、外键、Schema | `PASS`；144 实体、76 关系、238 事实、40 关系 hold、74 来源 |
| V2 Web Data 构建 | `PASS`；schema `v2-web-0.1` |
| Web Data 引用、状态、坐标 QA | `PASS`；24 地图节点、25 地图关系 |
| Curation 状态门禁 | `PASS`；公共层 35 条 auto approved，2 条进入 review queue |
| 虚构空间坐标安全 | `PASS`；科马拉、马孔多均无现实坐标 |
| `site/app.js` 语法 | `PASS` |
| 前端研究事实硬编码扫描 | `PASS` |
| 远程图片/外部资产扫描 | `PASS`；N2 不使用未经许可的图片资产 |
| `git diff --check` | `PASS` |
| 静态 HTTP 入口 smoke test | `PASS`；`site/` 与 Web Data 均返回 200 |

## 4. 手工审核矩阵

| 用户路径 | 内部状态 | USER 重点观察 |
|---|---|---|
| 首页打开地图 | 已实现 | 地图是否确实成为第一入口；首屏是否有文学感 |
| 国家→地点 | 已实现 | 国家层和地点层的关系是否自然、是否容易继续探索 |
| 现实地点 vs 虚构空间 | 已实现 | 两种空间是否足够清楚，虚构空间是否保留想象力 |
| 地点→作品/作者 | 已实现 | 地图是否真正导向阅读，而不是停留在节点浏览 |
| 作者→作品 | 已实现 | 作家页信息密度与作品入口是否合适 |
| 作品→研究层 | 已实现 | 研究层是否足够可回查，同时不打断普通阅读 |
| 搜索 | 已实现 | 是否能快速找到作者、作品和地点 |
| 时间线 | 已实现 | 文学节点是否为主体，历史背景是否足够克制 |
| 手机端 | 已建立响应式 | 地图、卡片、研究层在窄屏是否可读、可点击 |

## 5. 已知边界

- 地图是自包含的经纬度投影板，不接入外部地图瓦片；本轮优先验证文学地点交互和阅读路径；
- `帕拉尔`仍标记为 provisional，未进入 N2 主样本；
- 时间线中的历史背景节点保留 V1 当前候选状态，未生成自动准入的历史导语；
- `user_review` 推荐与 `hold` 比较已从公共策展层隔离，待 USER 决定，不阻塞静态原型运行；
- 图片、完整地图底图、全量策展和后端服务不属于本节点。

## 6. USER 审核重点

请重点审核：

1. “当代文学杂志 × 文学档案”的整体感觉是否成立；
2. 首页以地图为主入口是否符合产品方向；
3. 国家→现实地点/虚构空间的交互是否清晰；
4. 页面是否既能让普通读者继续阅读，又能在研究层回查依据；
5. 手机端基本阅读和点击路径是否可接受；
6. 当前是否允许进入 N2 之后的差量返修或下一阶段开发。

在 USER 通过 `V2-N2` 前，阶段 5—8、N3 和 N4 继续锁定。
