# V2 时间线完整化 QA

## 1. 任务信息

- 任务：`V2-S6-005` 时间线完整化
- 日期：2026-08-11
- 状态：`✅ DONE`
- 构建：`scripts/build_v2_web_data.py`
- 入口：`site/app.js` 的 `timelineGroups()` 与 `timelineItem()`

## 2. 时间线结构

- 10 个有 `source_minimum_status=meets` 内容卡的作家节点进入文学时期层；
- 28 个作品节点进入作品层；
- 5 个历史背景节点进入独立“历史背景”组，不生成地图节点；
- 作品优先使用 V1 事实中的出版年份；没有可用年份时保留内容卡时期或“待核查”；
- 作家优先使用内容卡时期区间；
- 页面按 `period_bucket` 分组，组内保留具体节点和研究状态；
- 作家和作品节点可回到对应档案页，历史背景节点进入关联研究节点回退。

## 3. 当前结果

| 节点类型 | 数量 |
|---|---:|
| 作家节点 | 10 |
| 作品节点 | 28 |
| 历史背景节点 | 5 |
| 合计 | 43 |

## 4. 研究状态门禁

- `candidate_for_staging_review`、`card_period_only` 等状态原样显示在研究状态位；
- 不把候选年份改写为无状态的确定性编年；
- 历史事件弱化为背景，不进入 V2 地图空间层；
- 时间线数据不在前端写入姓名、年份或作品事实。

## 5. 验证

- Web Data 构建与 QA：`PASS`；
- 时间线节点类型计数：10 / 28 / 5；
- `node --check site/app.js`：通过；
- `git diff --check`：通过。

