# V2 完整站策展草稿与 QA

## 1. 任务信息

- 任务：`V2-S5-002`
- 日期：2026-08-11
- 状态：`✅ DONE`
- 输入：V1 内容卡、V1 已审核地点关系、V2 页面覆盖清单
- 输出：`data/v2/curation/CURATION_ENTRIES.csv`、`data/v2/qa/V2_CURATION_BATCH_BUILD.json`
- 构建脚本：`scripts/build_v2_full_curation_drafts.py`

## 2. 批量结果

| 项目 | 数量 |
|---|---:|
| 策展文案总记录 | 51 |
| `auto_approved` | 47 |
| `user_review` | 0（原有推荐仍保留 1 条） |
| `hold` | 4 |
| 已有 N2 记录保留 | 16 |
| 本批新增 | 35 |

新增内容来自 17 个有 `meets` 内容卡的完整页、3 个研究缺口作品的 hold 记录，以及已审核地点关系的批量地点说明。N2 已有记录没有被覆盖。

## 3. 自动准入规则

- 作者与作品导语直接取 V1 内容卡“一句话简介”；
- 对论文观点或来源归属式表达保留原有限定语，不改写为无来源文学评价；
- 地点导语由 V1 `ASSOCIATED_WITH_PLACE` / `SET_IN` 关系描述聚合，不新增关系类型；
- 每条自动准入记录保留内容卡、关系 ID和来源 ID；
- 批量生成不修改 V1 SQLite，不改变 V1 事实、关系或审核状态。

## 4. Hold 记录

本批新增的 4 条 `hold` 记录包括：

- 《关于玛丽安娜的证词》；
- 《黑暗的职守》；
- 《金鸡》；
- 阿什格罗夫的地点导语。

前 3 项是 V1 明确的 `research_gap` 作品；阿什格罗夫的现实/虚构分类与可落点状态仍未确认。它们可以在研究层显示缺口，但不会进入公共阅读层。

## 5. Web Data 与状态隔离

重建后的 Web Data 统计：

- 144 个实体；
- 40 张 V1 内容卡；
- 238 条事实；
- 76 条正式关系；
- 24 个地图节点；
- 25 条地图关系；
- 51 条策展文案；
- 19 条展示选择；
- 2 条策展推荐。

Web QA `PASS`。公共 `curation` 只包含 `auto_approved`；独立 `review_queue` 保留 4 条 hold 文案、1 条 `user_review` 推荐和 1 条 hold 推荐，共 6 条记录。没有把策展推荐写入 V1 正式关系。

## 6. 结论

完整站页面所需的首轮证据绑定策展草稿已生成。后续 S5-003 只汇总需要 USER 判断的推荐、比较和其他强策展判断，不逐条打断用户。

`V2-S5-002 = ✅ DONE`，解锁 `V2-S5-003`。
