# 14_GIT_BACKLOG — WCD-09 Integration 前的工程工作项清单（给 Codex）

> 性质：本文件是**工程任务卡**，不是结论文件。每一项的「为什么」指向研究包 00–13 的对应证据；Codex 在 Integration 阶段逐项实现、逐项验证。
> 范围约束（DEC-055）：Codex 在 WCD-09 Integration 中可修改 `site/`（GeoJSON 资产、app.js、styles.css、index.html）、`docs/LICENSES.md`、构建/脚本目录；**不得**在本阶段改 Research Data / Curation（除非某项明确写明）。
> 所有标 `[Gate?]` 的项：先过 15_USER_DECISION_GATES.md 对应门禁，再实施。

优先级语义：**P0** = 违反 TASK-099 门禁/中立性目标，必须做；**P1** = 门禁明确要求的缺失；**P2** = 质量加固，与 P0/P1 同批做成本最低。

---

## A. 底图数据（Basemap）

| ID | 项 | 优先级 | 依赖 | 摘要（证据见） |
|---|---|---|---|---|
| CH-01 | 换用 Natural Earth **Admin 0 Countries 50m** 为新底图，并在获取时**记录版本号+获取日期**写进脚本常量与 CHANGELOG | P0 | **A-1;A-3** | 01 §1.2（110m 固有缺岛）、04 MAPSRC-05、11 §2.1 |
| CH-02 | 补入 **France（含法属圭亚那 polygon）**；仅在 C-8/C-3 通过后按选定 France polygon/part 以法国行政关联背景呈现（16 的 Article 73 DROM 行政证据为 `VERIFIED_PRIMARY`），不配成独立国家；不据此裁决边界、主权或范围 | P0 | **A-1;A-3;C-8;C-3;CH-01** | 01 §1.2 缺口 1、08、10 NAME-29 |
| CH-03 | C-8/C-6 选定后，按 08 成员集合处理小安的列斯 7 个主权国、特立尼达和多巴哥及荷/法/英/美加勒比候选几何；C-2 选定后才决定特立尼达和多巴哥 L2 处理；新增几何只有对应 10 NAME 行已有名称来源证据时才可暴露中文 tooltip/名称，`CANNOT_VERIFY` 时阻塞该项；不把 L1 几何自动转成 L2 交互 | P0 | **A-1;A-3;C-8;C-6;C-2;CH-01** | 01 §1.2、03、08、10、15 C-8/C-6/C-2 |
| CH-04 | 视窗外陆地策略：美国一侧按 **C-4**，南乔治亚/纳瓦萨/克利珀顿按 **C-7**；C-8 未决前只保留条件分支并准备图例说明 | P1 | **C-8;C-4;C-7** | 01 §1.2、03 §4、08、05 RISK-11; RISK-12; RISK-14 |
| CH-05 | 重建 `latin-america-countries.geojson` 的**处理脚本化**：NE 50m 原始文件 → **先 explode 为 polygon parts，再按目标窗口裁切/选择**，对每个保留 part 赋稳定 ID 与所属主权/属地 → 产物；**任何输入 part 不得在目标窗口外影响 fit**（加测试）；量化精度记录。脚本入库，产物 SHA-256 写进 CHANGELOG；消除 PROVENANCE_GAP 三要素（版本/日期/步骤） | P0 | **A-1;A-3** | 11 §1、00 §4 门禁一 |
| CH-06 | 保留 NE 110m 文件不删（回滚路径），新文件命名带版本（由 A-3 锁定） | P2 | **A-1;A-3;CH-01;CH-05** | 运维稳妥性 |

## B. 投影与几何（Projection）

| ID | 项 | 优先级 | 依赖 | 摘要（证据见） |
|---|---|---|---|---|
| CH-07 | 按 **B-1** 选定的 `candidate_id` 替换 `project()`；LAEA/Equirect/AEQD 必须参数化，画布使用统一缩放。验收分离为 `preserveAspectRatio`、方向代理 `ew_ns_scale_ratio`、完整 `principal_axis_ratio` 和 `area_factor`，不能互相替代 | P0 | **B-1;CH-01**（可并行） | 06 §4/§5、07、15 B-1 |
| CH-08 | 重新计算并核对 `LABEL_OVERRIDES`（GT/NI/CU/VE/EC/CL/UY 7 国像素值）与所选投影下的质心/标签位；任何像素常量都从所选投影**重导**，不沿用旧值 | P0 | **B-1;CH-07** | 06 §5、01 §4 item 6 |
| CH-09 | 重新核对 8 处 CSS 藏标签 place_id 在所选投影下是否仍需要隐藏；能改为数据驱动则改 | P0 | **B-1;CH-07** | 01 §4 item 6、styles.css 实测 |
| CH-10 | 换投影后重跑全部出画/入画计算：马德里/巴黎按 C-5 处理；检查新的 bbox 边缘没有新出画点或裁切伪影 | P1 | **B-1;C-5;CH-07;CH-11** | 09 render_px 列 |

## C. 地点与交互（Places & Interaction）

| ID | 项 | 优先级 | 依赖 | 摘要（证据见） |
|---|---|---|---|---|
| CH-11 | 马德里（V1-ENT-0129）、巴黎（V1-ENT-0373）出画可聚焦问题：按 **C-5** 选项改为非地图入口或图外列表芯片；保证键盘 Tab 不再进入不可见按钮（WCAG 2.4.7） | P0 | **C-5** | 01 §4 item 4、09（两行 MEDIUM） |
| CH-12 | 修复**巴西双实体**：`V2-GEO-BR` 与 `V1-ENT-0183` 合并为单一国家实体（或明确的 canonical 映射）；修复后验证：选中巴西 → 里约热内卢（parent=V2-GEO-BR）仍显示、国家页 children 非空 | P0 | 无（独立可做） | 01 §4 item 3、08 Brazil 行、09 |
| CH-13 | 修复地点坐标引用：17 个带坐标地点按 URL 类型分类——**10 个可定位弱引用需在 A-2 通过后转换；V1-ENT-0052 是第 11 个弱引用但 BLOCKED/CANNOT_VERIFY，不得转换**（9 处 GeoNames search/advanced-search URL 中不含恰帕斯 + V1-ENT-0127 帕拉尔镜像站），逐条换成永久 GeoNames 实体页 URL；**6 处已是永久源只做 URL 规范化**。V1-ENT-0052 保持 `CANNOT_VERIFY`，不得编造或强制转换。这是 Data/Curation 引用字段变更，必须走 **A-2**，不得用 D-1 授权 | P1 | **A-2** | 09（三分支清单：10 可转换、1 BLOCKED、6 规范化）；M-04 重建清单 |
| CH-14 | C-8 选定后按 C-1 的无文学数据 L2 处理选择，增加无内容背景图例项「当前暂无收录作家或作品」，并覆盖 08 的相应 L1 条件候选行 | P0 | **C-8;C-1** | 01 §4 item 5、08、TASK-099 门禁原文 |
| CH-15 | 波多黎各 tooltip 双语化；描述性地位说明仅在 **C-6/C-8** 选定后按 RISK-05 落地，且必须在 C-6 记录槽留存并核验 primary URL、标题、检索日期；证据不足时只做中文名称、不写地位；不把 status 文字写成主权结论 | P1 | **C-6;C-8;CH-16** | 10 NAME-24、05 RISK-05 |

## D. 中立性与命名（Neutrality & Naming）

| ID | 项 | 优先级 | 依赖 | 摘要（证据见） |
|---|---|---|---|---|
| CH-16 | **消除所有 EN-only tooltip**：福克兰采用 D-1 选定的双名形态（官方名源未核验前不预设次序）；其余背景属地按 C-6/C-8 选定的标签策略中文化；新加勒比几何只有对应 10 NAME 行已有名称来源证据时才可暴露中文 tooltip/名称，`CANNOT_VERIFY` 时阻塞该项 | P0 | **D-1;C-6;C-8** | 05 RISK-01、10 NAME-06、10 NAME-31…NAME-37 |
| CH-17 | 争议区域及未决勘界段渲染：按 05 逐案策略；D-2 决定线型，D-3 决定单案注记/处置，D-4 决定通用披露，三者互不替代；RISK-20 不得把普通实线描述成最终勘界完成 | P1 | **D-2;D-3;D-4** | 05 RISK-01; RISK-02; RISK-03; RISK-19; RISK-20 |
| CH-18 | 图 aria 描述/window title 与实际范围一致；**C-8 未圈定前**使用范围中性文案，裁决后再落精确范围 | P2 | **C-8** | 05 RISK-18 |
| CH-19 | E-1 仅登记中国大陆发布场景的三问待专业人员评估；本批不实施大陆发布技术变更 | P2 | **E-1** | 11 §2.7、15 E-1 |

## E. 许可与署名（License & Attribution）

| ID | 项 | 优先级 | 依赖 | 摘要（证据见） |
|---|---|---|---|---|
| CH-20 | `docs/LICENSES.md` 增补三要素：NE 上游 release 版本号、获取日期、处理步骤（指向 CH-05 的脚本）；模板见 13 §5 | P0 | **A-3;CH-05** | 11 §1/§4 |
| CH-21 | About 页署名行升级为 13 §5 固定格式（含版本+日期；采用 geoBoundaries 则逐文件追加）；地点坐标 GeoNames CC BY 4.0 署名核入；D-4 通过后同步通用署名/免责声明文案 | P1 | **A-1;A-2;A-3;D-4;CH-20** | 11 §4、13、15 D-4；A-1 OPT2 的 geoBoundaries 逐文件署名 |
| CH-22 | 若 C-3/C-4/C-6/C-7 或 D-2/D-3/D-4 决定引入新几何/争议叠层资产：逐文件许可复核，`LICENSE_UNCLEAR` 不入库 | P2 | **A-1;C-3;C-4;C-6;C-7;D-2;D-3;D-4** | 11 §2.3 |

## F. 测试与验证（QA）

| ID | 项 | 优先级 | 依赖 | 摘要 |
|---|---|---|---|---|
| CH-23 | 现有浏览器测试分别断言总背景数（L1）、唯一可交互国家 code 数、无内容背景数和重复 code；C-1/C-6 选择影响无内容和 L2 计数；`.available` 逻辑不得改成 36+ | P0 | **A-1;C-8;C-1;C-6;CH-01;CH-03** | 06 §5、08、tests/browser/public-product.spec.cjs:46-50 |
| CH-24 | 新增机械校验：几何 bbox、无 EN-only tooltip、无内容图例、Tab 可见性和 RISK-20 未决勘界中性呈现；投影验收分开测试 `preserveAspectRatio`、所选候选 `ew_ns_scale_ratio`（方向代理）、`principal_axis_ratio`（完整形状）和 `area_factor`，不能将等比画布当作面积因子验收；C-1/C-4/C-7/D-2/D-3 决定相应敏感呈现或新增几何 QA 是否实施 | P1 | **B-1;A-1;C-4;C-5;C-7;C-8;C-1;D-2;D-3;CH-07;CH-11** | 09E 输入、06、07、05 RISK-20、B-03 |
| CH-25 | 色彩对比度/色盲专项（09E 范围）：状态不只靠颜色（形状+光标+aria 已有），补足对比度量化核验 | P1 | **C-8;CH-14** | 01 §5 |

---

## 附：CH-13 坐标来源分类（M-04 重建）

**10 个可定位弱引用（A-2 通过后转换）**（9 处 GeoNames search/advanced-search URL 中不含恰帕斯 + 1 处镜像站）：
`V1-ENT-0022`（里约）、`V1-ENT-0053`（科米坦）、`V1-ENT-0054`（伊斯特佩克）、`V1-ENT-0056`（墨西哥城）、`V1-ENT-0057`（圣加布里埃尔）、`V1-ENT-0098`（阿卡塔卡/locality）、`V1-ENT-0125`（利马）、`V1-ENT-0127`（帕拉尔/镜像）、`V1-ENT-0128`（圣地亚哥）、`V1-ENT-0129`（马德里）——仅在 A-2 通过且逐行证据齐备后换成正确永久 GeoNames 实体页 URL；V1-ENT-0052 是第 11 个弱引用，保持 BLOCKED/CANNOT_VERIFY，找到并核验正确永久实体前不得转换或 promote。清单里**无街道类**；V1-ENT-0052 是 region（恰帕斯），未确认项 ID 是 **V1-ENT-0052，不是 V1-ENT-0051**。

**仅需 URL 规范化、不需转换的 6 处已是永久源**：`V1-ENT-0126`(阿雷基帕)、`V1-ENT-0153`(比库尼亚)、`V1-ENT-0370`(布宜诺斯艾利斯)、`V1-ENT-0371`(蒙得维的亚)、`V1-ENT-0372`(哈瓦那)、`V1-ENT-0373`(巴黎)。

## 建议实施顺序（Codex Integration）

1. **15 中 A-1…E-1 全部由 USER 裁决**→ 记录进 DECISIONS。其中 **C-8（L1 范围元决策）**须先于 CH-02、CH-03、CH-04、CH-14 等依赖范围的项；CH-11 仅依赖 C-5；CH-07 依赖 **B-1**。
2. CH-01 与 CH-05（新数据源+脚本化）与 CH-07（新投影）并行（均需对应稳定 ID 通过）；
3. CH-08、CH-09、CH-10、CH-11 跟进：CH-08、CH-09 重导标签（依赖 CH-07）；CH-11（马德里/巴黎出画，C-5）与其产出的出画判定被 CH-10 的重跑吸收——先做 CH-11 的选项落地，再做 CH-10 的出画复检（避免顺序颠倒导致误判）；CH-02、CH-03、CH-04（依赖 C-8）与 CH-12、CH-14 跟进；
4. CH-16、CH-17（D-1/D-2/D-3/D-4 落地）、CH-20、CH-21、CH-22（如启用）；
5. CH-13 走数据流程（正式 Gate，非「知情」）；CH-23、CH-24、CH-25 收口 → 09F 独立终审。

---

## 附 2：CH 验收标准（B-03 补录；Codex 逐项 PASS/FAIL，09F 复核）

每项给出**可观察验收条件**与**失败条件**；未全部 PASS 前不标记对应 CH DONE。

| CH | 可观察验收标准（PASS 条件） | 失败条件（FAIL） |
|---|---|---|
| CH-01 | `site/assets/` 出现 A-1 选定的 NE 50m 产物；A-3 版本号+获取日期写进脚本常量与 CHANGELOG；feature 覆盖 08 在 C-8/C-6 选定的缺项 | 版本/日期缺失；把候选范围或来源写成已决 |
| CH-02 | C-8/C-3 通过后，France polygon（含法属圭亚那）在 L1 按选定 France polygon/part 以法国行政关联背景呈现；16 行政证据为 `VERIFIED_PRIMARY`；不据此裁决边界/主权/范围，未配成独立国家；tooltip 无 EN-only | C-3 未决就实施；法属圭亚那缺失或配色为独立国 |
| CH-03 | C-8/C-6 通过后，08 C-6 集合逐一在 L1 可见；新增加勒比几何只有对应 10 NAME 行已有名称来源证据时才可暴露中文 tooltip/名称，NAME 行为 `CANNOT_VERIFY` 时必须阻塞该项；L2 仍按内容事实，不把 L1 TRUE 改成交互 | 任一成员缺失；没有对应 NAME 来源证据仍暴露中文名称；`CANNOT_VERIFY` 行未阻塞；08 把条件状态写成已授权或自动交互 |
| CH-04 | C-4/C-7 分支均有可观察结果：范围外陆地有明确图例/关于页说明，或经许可后有背景几何 | 范围外陆地渲染为海色且无说明；未有 C-4/C-7 决议即实施 |
| CH-05 | 脚本可从 A-1 选定的 NE 源重建产物且 SHA-256 复现；每个保留 part 有稳定 ID+归属；fit 不受窗口外 part 影响 | 产物 SHA 漂移；part 归属缺失；版本/日期/处理步骤任一缺 |
| CH-06 | 110m 旧文件保留可回滚；新文件名带 A-3 锁定的版本 | 旧文件被删或新文件名无版本 |
| CH-07 | 按 B-1 选定 candidate_id 参数化投影；画布 `preserveAspectRatio`/统一缩放通过；该候选的 `ew_ns_scale_ratio`、`principal_axis_ratio`、`area_factor`（以及相对中心列）分别落入 07/metrics 范围 | 用 LAEA 范围验收 AEQD/Equirect；以画布属性替代所选候选的 `area_factor` 检查；忽略完整主轴比或仍有 x/y 非统一缩放 |
| CH-08 | `LABEL_OVERRIDES` 与 B-1 所选投影重导一致；旧像素常量未沿用 | 任一标签沿用旧值或仅视觉猜测 |
| CH-09 | CSS 藏标签核对完；无法数据驱动者有 place_id 级证据 | 标签在新投影下错位/漏隐藏 |
| CH-10 | 重跑所选投影的出画清单；无新出画点或裁切伪影，C-5 对马德里/巴黎已落地 | 出画点未登记或仍存在不可见可聚焦按钮 |
| CH-11 | C-5 方案落地；马德里/巴黎不再作为不可见地图按钮进入 Tab 序列 | 出画点仍可聚焦到不可见元素 |
| CH-12 | 巴西 canonical 映射唯一；选巴西→里约（parent=V2-GEO-BR）仍显示；国家页 children 非空 | 重复 code 仍覆盖 parent；里约丢失或 children 空 |
| CH-13 | A-2 通过后，10 个可定位弱引用逐条换永久 ID；V1-ENT-0052 保持 BLOCKED/CANNOT_VERIFY；6 个永久源只规范化 | 用 D-1 或无 A-2 修改 Data/Curation；编造恰帕斯 ID；把阻塞项 promote 为已核验 |
| CH-14 | C-8 通过后，图例含「当前暂无收录作家或作品」且覆盖对应 L1 无内容背景 | 图例缺条目或把 L2 交互状态当作 L1 |
| CH-15 | C-6/C-8 通过后波多黎各 tooltip 可中文化；任何描述性地位说明须有 C-6 记录槽 primary URL/标题/检索日期并经核验，否则只保留中文名称、不写地位；不含主权结论 | tooltip 仍 EN-only、缺少 C-6 primary 记录或写成未经核验的地位结论 |
| CH-16 | D-1 通过后福克兰按选定双名；C-6/C-8 适用的背景属地 tooltip 中文化；新加勒比几何只有对应 10 NAME 行已有名称来源证据时才可暴露中文 tooltip/名称，NAME 行为 `CANNOT_VERIFY` 时必须阻塞该项；全站无 EN-only | 未有 D-1 就定序；没有对应 NAME 来源证据仍暴露中文名称；`CANNOT_VERIFY` 行未阻塞；扫描 DOM 仍现 EN-only |
| CH-17 | D-2 线型、D-3 逐案处置、D-4 通用披露分别有记录并落地；RISK-19 与 RISK-20 使用 D-2/D-3/D-4，RISK-20 不得宣称最终勘界完成 | 用通用免责声明替代逐案判断；RISK-19 与 RISK-20 错挂 D-1，或将普通实线说成最终勘界 |
| CH-18 | C-8 前使用范围中性 aria/title；C-8 后与实际范围一致 | 描述夸大或把宽范围写成已授权 |
| CH-19 | E-1 只记录三问 CANNOT_VERIFY 与专业评估前置；本批无大陆发布技术实现 | 出现未经专业评估的确定合规结论或大陆技术发布 |
| CH-20 | A-3 版本三要素入库并随 CHANGELOG；provenance 脚本可重取 | 版本/日期/步骤任一缺 |
| CH-21 | 署名/来源表格与源码一致；A-1 OPT2 选用 geoBoundaries 时逐文件署名；A-2 坐标来源字段引用检查通过 | 引用与实际不一致或把未知许可写成禁止 |
| CH-22 | C-3/C-4/C-6/C-7 或 D-2/D-3/D-4 触发的新资产逐文件入许可证据；LICENSE_UNCLEAR 不入库 | 新资产许可不明仍入库；遗漏对应 Gate 依赖 |
| CH-23 | 测试分别断言总背景数、唯一可交互 code 数、无内容数、重复 code；`.available` 逻辑未被改成 36+ | 把 L1 背景数当 L2 交互数 |
| CH-24 | 校验套件含几何 bbox、tooltip、图例、Tab 可见性、C-4/C-7 新增几何 QA、RISK-20 未决勘界中性措辞，以及 `preserveAspectRatio`、所选投影 `ew_ns_scale_ratio`、`principal_axis_ratio`、`area_factor` 四项独立检查 | 任一检查缺失；把等比画布误当成面积因子为 1、把方向代理当成完整形状，或把 RISK-20 普通实线写成最终勘界 |
| CH-25 | 状态不只靠颜色（形状+aria 已有）；对比度量化达标 | 存在仅靠颜色的状态或对比度不达标 |

---

## 附 1：CH-13 坐标来源分类（M-04 重建）

**10 个可定位弱引用（A-2 通过后转换）**（9 处 GeoNames search/advanced-search URL 中不含恰帕斯 + 1 处镜像站）：
`V1-ENT-0022`（里约）、`V1-ENT-0053`（科米坦）、`V1-ENT-0054`（伊斯特佩克）、`V1-ENT-0056`（墨西哥城）、`V1-ENT-0057`（圣加布里埃尔）、`V1-ENT-0098`（阿卡塔卡 locality）、`V1-ENT-0125`（利马）、`V1-ENT-0127`（帕拉尔/镜像）、`V1-ENT-0128`（圣地亚哥）、`V1-ENT-0129`（马德里）。仅在 A-2 通过且逐行证据齐备后换成正确永久 GeoNames 实体页 URL；V1-ENT-0052 是第 11 个弱引用，保持 BLOCKED/CANNOT_VERIFY，找到并核验正确永久实体前不得转换或 promote。

**仅需 URL 规范化、不需转换的 6 处已是永久源**：`V1-ENT-0126`（阿雷基帕）、`V1-ENT-0153`（比库尼亚）、`V1-ENT-0370`（布宜诺斯艾利斯）、`V1-ENT-0371`（蒙得维的亚）、`V1-ENT-0372`（哈瓦那）、`V1-ENT-0373`（巴黎）。

## 建议实施顺序（Integration）

1. USER 先裁决 15 ledger 的相关 IDs；C-8 必须先于 C-1…C-7，A-2 必须先于 CH-13，B-1 必须先于 CH-07、CH-24。
2. CH-01 与 CH-05（A-1/A-3）与投影研究可并行；实际实现等 B-1 后再锁定 `candidate_id`。
3. CH-08、CH-09、CH-10、CH-11 跟进：先落 C-5，再重算所选投影的标签与出画清单；CH-02、CH-03、CH-04、CH-14 只在 C-8/C-3/C-4/C-6/C-7 对应决议后实施。
4. CH-16、CH-17（D-1/D-2/D-3/D-4）、CH-20、CH-21、CH-22 按各自 Gate 落地；CH-13 单独走 A-2 的 Data/Curation 审批路径。
5. CH-23、CH-24、CH-25 收口后进行 09F 独立终审；任何 `CANNOT_VERIFY` 不得在交接中写成已核验。
