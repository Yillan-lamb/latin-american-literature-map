# 阶段 1 外部 AI 派单总览

生成时间：2026-08-04

当前主任务状态以项目根目录 `TASKS.md` 为准。本目录只保存本地派单材料，不上传公开 GitHub。

所有外部 AI 开工前必须完整阅读项目根目录 `PROJECT_CHARTER.md`。该文件未经用户明确授权不得修改、移动、重命名或删除。

## 执行顺序

1. 先把 `V1-S1-003_正式来源ID回填/PROMPT.md` 交给负责整理的外部 AI。
2. 如果回填任务终态为 `done`，把它的完整交付目录与 `V1-S1-006_正式ID回填独立复核/PROMPT.md` 一起交给第二个 AI。
3. 第二个 AI 必须与回填 Worker 不同；优先使用不同 AI 软件或不同模型。至少不得使用原 Worker 的同一对话继续自审。
4. 独立复核完成后，把回填包和复核包一起交回 Codex。
5. Codex复核通过后，才会把结构化候选放入 `data/staging`，并完成阶段 1 汇总与缺口报告。

如果任一任务终态为 `blocked` 或 `failed`，不要自行扩大范围，直接把该任务目录交回 Codex。

## 已由 Codex 完成

- N1 节点审核登记；
- `SRC-0001`~`SRC-0006` 正式编号；
- 公开来源登记表；
- R2 OCR/建档任务包最终验收。

## 禁止上传

原始 PDF、EPUB、`inputs/`、完整文字层、整书 OCR、账号配置和任何从原书复制的大段文本均不得上传公开仓库或公开链接。
