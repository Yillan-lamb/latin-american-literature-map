# 拉丁美洲文学地图：AI 协同研究与内容数据库工作计划

版本：0.3.1

日期：2026-08-04

状态：实施支持计划；动态进度见 `TASKS.md`

> 本计划受仓库根目录 `PROJECT_CHARTER.md` 约束。总章程规定长期目标和不可突破的边界；本文件只负责解释实施方法。未经用户明确授权，任何 AI 不得修改总章程。

## 一、项目目标

本项目面向中文读者建设一套“研究型拉丁美洲文学数字地图”。项目先完成来源可追溯的内容数据库，再以该数据库驱动地图、时间轴、作家关系图、主题探索和专题展览。

V1 只完成内容和数据底座，不开发网站或小程序。目标规模为：

- 约 10 位核心作家、30 部作品、10 个主题；
- 约 6 个文学运动、12 个文学或历史事件；
- 至少 150 条有文献依据的实体关系；
- 覆盖约 7 个国家或地区，兼顾西班牙语、葡萄牙语文学以及不同性别、时代和体裁；
- 以用户提交书籍为研究底座，并允许 AI 扩展权威学术及公共资料。

## 二、内容框架

### 2.1 首版策展方向

首版采用“均衡文学地图”路线，同时保留“权力与正义”“形式与先锋”作为后续专题扩展方向。

默认核心作家候选：

博尔赫斯、加西亚·马尔克斯、科塔萨尔、略萨、聂鲁达、克拉丽丝·李斯佩克朵、胡安·鲁尔福、阿莱霍·卡彭铁尔、埃莱娜·加罗、罗萨里奥·卡斯特利亚诺斯。

默认十大主题：

1. 迷宫、时间与无限
2. 魔幻现实与现实观
3. 权力、独裁与国家暴力
4. 记忆、遗忘与历史
5. 革命、流亡与边界
6. 城市与现代性
7. 乡土、边疆与民族身份
8. 语言实验与叙事形式
9. 性别、身体与主体
10. 正义、复仇与审判

### 2.2 知识模型

核心实体统一为：

- 作家 `Author`
- 作品 `Work`
- 地点 `Place`
- 文学运动 `Movement`
- 历史或文学事件 `Event`
- 主题 `Theme`
- 资料来源 `Source`
- 证据片段 `Evidence`

所有实体使用稳定 ID、规范名称、原文名称、别名、时间、地域、语言和审核状态。

关系采用统一结构：

`主体 → 受控关系词 → 客体`

每条关系至少保存主体与客体、关系类型、研究说明、证据、来源定位、可信度、争议状态和审核状态。涉及“影响”“代表”“源于”等解释性关系时，原则上需要两项独立可靠来源；证据不足时必须标记为“待证”或“争议”，不得写成确定事实。

### 2.3 三层数据交付

- 规范数据库：SQLite 作为唯一主数据；
- 人工查看层：Excel/CSV 总表，用于用户抽样查看和批注；
- 网站数据层：带 `schema_version` 的 JSON，供后续地图、时间轴和关系图直接读取。

Excel 和 JSON 均由主数据库生成，不分别人工维护。用户意见先进入变更记录，再由数据整合 AI 更新主数据库。

短引文和页码证据保留在内部审核层；未来公开数据默认只包含释义、引用信息和必要的短摘录，不公开传播大段版权文本。

## 三、用户节点审核机制

用户不审核每一批书、每一条关系或每一次 AI 内部修改，只在下列四个会影响全局方向的节点作决定。

| 节点 | 用户看到的内容 | 用户需要决定什么 | 节点之间 AI 的权限 |
|---|---|---|---|
| N1 范围基线 | 项目章程、来源策略、作家/作品/主题候选矩阵 | 是否认可 V1 边界和优先级 | 可自动接收资料、补充检索并修正候选清单 |
| N2 模型试点 | 两位作家的试点数据库、关系样例和中文内容样稿 | 是否认可 Schema、关系强度和文字风格 | 通过后可自动执行全部批次，不逐批等待确认 |
| N3 V1 候选版 | 完整数据包、覆盖矩阵、争议清单和质量报告 | 是否存在必须调整的方向性问题 | 可自动纠错、补证、去重和重新导出 |
| N4 正式发布 | 发布摘要、版本差异、未解决问题和 GitHub 变更 | 是否发布该版本或进入网站阶段 | 批准后创建版本标签并进入下一阶段 |

只有以下情况允许 AI 在节点之间提前中止并请求用户决定：

- 资料或范围出现相互冲突且会改变项目定位；
- 需要处理隐私、付费材料、版权风险或其他敏感内容；
- 需要删除、公开或不可逆地迁移大量数据；
- 原计划的数量、成本或时间需要显著扩大；
- 连续两轮 AI 纠偏仍无法通过质量门槛。

普通缺页、译名冲突、资料重复、关系证据不足等问题由 AI 自行处理并写入问题清单，不占用用户审核。

## 四、实施阶段

### 阶段 0：项目章程与任务源

建立项目章程、数据目录、来源规则、受控词表和任务清单。所有后续任务从统一任务源领取，记录负责人、依赖、输入、输出、验证规则和状态。

本阶段已经形成项目章程、研究与 OCR 规范、`TASKS.md`、外部 AI 分工手册、标准交接模板和 N1 审核包。跨平台外部 AI 的状态不直接写入主任务源，由 Codex 根据其 `STATUS.md` 和 `HANDOFF.md` 统一收口。

阶段末进入 N1。

### 阶段 1：资料接收与来源盘点

每份书籍或外部资料建立来源档案，记录版本、语言、页数、文字层、章节结构、引用方式和使用限制。扫描件先检查 OCR 质量，原始文件保持不变且不上传公开仓库。

外部研究按以下优先级补充：

1. 用户提供的文学史、专著和原著；
2. 学术专著、论文及大学研究资源；
3. 图书馆、出版社、作家基金会等权威资料；
4. 普通网页仅作为线索，不作为解释性观点的唯一依据。

本阶段由 AI 内部验收，不单独要求用户审批。

### 阶段 2：知识模型与试点

先以博尔赫斯和李斯佩克朵为跨语言试点，处理约 6 部作品和 20—30 条关系，验证姓名与译名、来源定位、关系粒度、中文摘要风格以及未来可视化接口。

阶段末进入 N2。通过后冻结 V1 Schema；后续结构调整必须建立迁移记录。

### 阶段 3：多批次并行研究

每批处理 2—3 本书或 2—3 位作家。研究 Agent 只能向自己的暂存区提交成果，不直接编辑主数据库。每批产出：

- 新实体和候选关系；
- 研究摘要与证据定位；
- 来源冲突和不确定项；
- 自动验证报告；
- 结果摘要和变更说明。

另一名 Reviewer Agent 对该批次交叉审核。通过后由数据整合 Agent 单线程合入主数据库。批次之间自动继续，无需用户逐批确认。

### 阶段 4：综合编目与内容深化

AI 自动完成译名和别名合并、重复实体清理、关系词统一、观点冲突处理、作家卡片、作品卡片、主题说明、探索路径和策展覆盖分析。

完成后生成 V1 候选版并进入 N3。

### 阶段 5：V1 冻结与发布

根据 N3 意见完成最后一轮修正，生成规范数据库、审核表格、网站 JSON、数据字典、来源目录、决策记录、变更日志和质量报告。

进入 N4；通过后创建正式版本标签并推送到 GitHub。新增资料通过后续版本增量导入，不静默改写已发布记录。

### 阶段 6：数字展览网站

V2 再开发数据驱动的响应式网站，包含首页、文学地图、文学时间轴、作家与作品、主题探索、专题研究、阅读路径和项目说明。网站只消费已冻结的 JSON，不在页面代码中硬写研究内容。

V3 可增加知识网络、个性化阅读推荐、AI 问答和小程序版本。

## 五、AI 与子 Agent 协同机制

### 5.1 组织方式

默认采用“一名项目经理 AI + 最多三名并行 Worker/Reviewer”的 Wave 模式；平台并发能力变化时可调整，但同一数据共享域只允许一名 Agent 写入。

- 项目经理 AI：维护任务源、分组、派工、巡检、验收、GitHub 收口和节点报告；
- 资料 Agent：处理书目、文本质量、章节结构和证据定位；
- 研究 Agent：提取实体、关系、观点和主题；
- Reviewer Agent：核对原文、页码、关系强度和中文释义；
- 数据整合 Agent：单线程更新主数据库并生成导出物；
- 策展审核 Agent：检查国家、语言、性别、体裁和时代分布。

同一 Agent 不得审核自己的研究批次。共享 Schema、受控词表、主数据库和版本记录不得并行修改。

### 5.2 Wave 运行规则

1. Wave 0 串行确定 Schema、词表和验收规则；
2. Wave 1 按来源并行建立资料档案；
3. Wave 2 按作家或书籍并行提取到暂存区；
4. Wave 3 交叉审核，作者与审稿者必须不同；
5. Wave 4 单线程整合主数据库；
6. Wave 5 并行进行引用、数据一致性和策展平衡审计；
7. 项目经理汇总结果，只有到达 N1—N4 时才唤起用户。

每张任务卡必须写明输入文件、来源 ID、允许范围、禁止范围、Schema 版本、交付物、引用要求、验证项和终止条件。Worker 必须提交状态、结果摘要、变更摘要和验证结果后才算完成。

### 5.3 自主级别

节点之间默认允许 AI：

- 自动启动下一批安全可并行任务；
- 自动交叉审核并要求原 Worker 修订；
- 自动合并已通过验证的暂存成果；
- 自动更新任务状态、决策记录和变更日志；
- 自动将完成的独立修改提交并上传 GitHub；
- 自动降低并发、重启失败任务或将不影响主线的问题延期。

涉及公开发布、重大范围变化或不可逆操作时，不自动越过用户节点。

## 六、GitHub 与版本管理

- GitHub 仓库为公开仓库，但用户提供的原始书籍、扫描件、版权文本、密钥和私有研究材料不得提交。
- 每个可独立说明的修改形成一个语义清晰的提交，并在完成验证后上传 GitHub。
- 日常低风险改动由 AI 完成审查和合并；版本发布与方向性变更在 N4 由用户确认。
- 每次发布必须同步更新 `README.md`、`拉丁美洲文学地图_项目决策记录.md` 和 `CHANGELOG.md`。
- 版本号采用语义化规则：破坏性结构变化升主版本，新增兼容内容升次版本，纠错和小调整升补丁版本。
- 每个版本记录新增、修改、删除、决策变化、数据迁移、验证结果和未解决问题。

## 七、质量门槛

V1 必须达到：

- 所有实体 ID 唯一，关系两端均引用有效实体；
- 所有研究性摘要和关系均有可定位来源；
- 本地书籍证据具有章节、页码或电子位置，网络资料具有 URL 和访问日期；
- 所有关系均经过第二名 AI 的证据一致性检查；
- 争议性或影响性判断具有双来源，或明确标记争议；
- 不存在未说明的译名合并、重复作家或重复作品；
- JSON、审核表和主数据库的数量及版本一致；
- 至少覆盖 7 个国家或地区、3 位女性作家，并包含葡萄牙语文学；
- 每位作家至少有两项可靠来源；每部核心作品至少有书目信息和一项研究依据；
- 质量报告列明已解决问题、未解决问题、资料空缺和下一版建议。

## 八、默认假设

- 最终内容以中文呈现，同时保留西班牙语、葡萄牙语原名及规范重音符号；
- 用户分批提交书籍，资料较多时按批次扩展，不改变核心 Schema；
- V1 不包含 HTML、小程序、部署、AI 问答或用户账户；
- 用户只在 N1—N4 节点审核方向和代表性样本；
- 无法确认的信息保持为空或进入问题清单，不由 AI 猜测补全；
- 第一批资料到位前不承诺固定工期，执行进度以 Wave 和节点报告衡量。
