# 拉丁美洲文学地图：项目决策记录

版本：0.11.0

最后更新：2026-08-11

## 使用规则

本文件记录项目中会影响范围、数据结构、协作方式、公开成果或后续成本的重要决策。每项决策使用固定编号；后续如有变更，不删除旧决定，而是将其标记为“已取代”，并新增一条决策说明取代关系和迁移影响。

每次版本发布必须检查：

- 本版本是否新增或改变了决策；
- 改变的决策影响了哪些数据、文档或页面；
- 是否需要迁移、重新研究或重新导出；
- `CHANGELOG.md` 是否同步记录实际修改。

## 决策总览

| 编号 | 主题 | 已选择方案 | 状态 |
|---|---|---|---|
| DEC-001 | 地域范围 | 拉丁美洲 | 生效 |
| DEC-002 | V1 成果 | 内容数据库 | 生效 |
| DEC-003 | 用户参与 | 关键节点审核 | 生效，取代“逐阶段审核” |
| DEC-004 | 首版规模 | 精品首版 | 生效 |
| DEC-005 | 资料边界 | 开放研究扩展 | 生效 |
| DEC-006 | 数据交付 | 三层数据包 | 生效 |
| DEC-007 | 策展主线 | 均衡文学地图 | 生效 |
| DEC-008 | AI 协同 | PM + 子 Agent 多 Wave 闭环 | 经 DEC-018 调整当前执行模式 |
| DEC-009 | GitHub 与版本 | 公开仓库、每次完成修改后上传 | 已被 DEC-015 取代 |
| DEC-010 | AI 用量与跨平台分工 | 外部 AI 做基础处理，Codex 做结构、判断与整合 | 生效 |
| DEC-011 | 首个外部任务包验收 | R2 通过并登记为先行证据，不提前解锁阶段 1 | 生效 |
| DEC-012 | N1 范围基线 | 用户批准建议范围，阶段 1 解锁 | 生效 |
| DEC-013 | 首批来源编号 | 按接收顺序分配 SRC-0001~SRC-0006 | 生效 |
| DEC-014 | 项目总章程 | 建立用户锁定的 `PROJECT_CHARTER.md` 作为最高规范 | 生效 |
| DEC-015 | GitHub 批量上传 | 仅在用户明确通知后提交并上传 | 已被 DEC-031 取代 |
| DEC-016 | 阶段 2 试点选择 | 双作家各三部作品，先用八个合法开放来源做材料检查 | 生效；详细页段门槛被 DEC-017 调整 |
| DEC-017 | 证据定位粒度 | 书籍/论文/网页来源级定位，页码章节可选 | 生效，修改章程至 1.2.0 |
| DEC-018 | AI 协作效率 | 双 Agent 单任务队列、LITE/FULL、差量返修、机械检查 | 生效；任务粒度经 DEC-019 调整 |
| DEC-019 | 外部任务粒度与审核门禁 | WorkBuddy 里程碑任务包，Codex只审关键门禁 | 生效，调整 DEC-018 的派单粒度 |
| DEC-020 | 阶段 2 正式来源编号 | 七个已通过网络来源编号为 SRC-0007~SRC-0013 | 生效 |
| DEC-021 | 外部执行方更换 | 后续任务由新外部 AI EXT-AI-02 执行，WorkBuddy 停止接收新任务 | 生效 |
| DEC-022 | 阶段 2 候选与实体分层 | 候选层通过；作品、作品集、版次和改编分层后再进暂存 | 生效 |
| DEC-023 | N2 前试行 Schema | 固定本轮实体层、11 个关系词、证据分组和迁移门禁 | 生效至 N2 审核 |
| DEC-024 | 阶段 2 关系候选准入 | R1 关系与事实包通过；确认关系和待补证关系分层进入试点包 | 生效至 N2 审核 |
| DEC-025 | 试点数据包预组装分工 | 外部 AI 生成 PRE 层三格式预览，Codex负责正式暂存和最终包 | 生效至 N2 审核 |
| DEC-026 | PRE 数据包准入与正式整合口径 | PRE 包通过；逻辑可重复构建、15/11 分层、9 张 N2 卡片及来源关联表 | 生效至 N2 审核 |
| DEC-027 | N2 正式暂存包 | 28 实体、15/11 关系分层、52 事实、9 卡片、11 来源进入用户节点审核 | 待 N2 用户确认 |
| DEC-028 | N2 通过与阶段 3 批次 | 冻结 Schema 0.2；剩余 8 位作家分为 B01—B03 三个里程碑包 | 生效 |
| DEC-029 | B01 墨西哥批次准入 | R0 通过；24 个关系 eligible、7 个 hold；同名分层与来源替代按 REVIEW 处理 | 生效 |
| DEC-030 | B02 文学爆炸/加勒比批次准入 | R1 通过；基础事实阶段 4 集中补核；概念、人物/作品及改编链按层处理 | 生效 |
| DEC-031 | V1 GitHub 节点式自动发布 | S3、N3、N4 门禁满足后由 Codex 自动审计、提交、推送和标记 | 生效，取代 DEC-015 |
| DEC-032 | B03 准入与阶段 3 收口 | R1 通过；59/29 关系分层；基础事实、事件兼容性和政治诗歌证据进入阶段 4 | 生效 |
| DEC-033 | A03 来源编号与关系争议收口 | 正式来源扩至 SRC-0072；29 个 hold 保留；恢复 1 个 SET_IN 候选；事件关系进入 N3 提案 | 生效 |
| DEC-034 | A04 收口与 A05 差量补包 | A04 R1 通过；不调整 150 门槛；审计 legacy 117，并补 7 部研究缺口与一项权威书目 | 生效 |
| DEC-035 | A05 收口与 A06 预组装 | A05 R1 通过；legacy 净新增 0；缺口不重置；外部 AI 只做 PRE 组装，N3 决定 75/150 与事件兼容性 | 生效 |
| DEC-036 | A06 正式候选包与 N3 门禁 | PRE R1 通过；Codex 分配 SRC-0073/0074、规范 144 实体并生成 CSV/JSON/SQLite/Excel；四项方向性问题留给 USER | 已由 DEC-037 收口 |
| DEC-037 | N3 通过与 Schema 0.3 | USER 选择 B/A/A/A；V1 门槛改为 75，新增 work→event 的 BASED_ON_EVENT，author→event 不建模，政治诗歌维持分层 | 生效 |
| DEC-038 | 阶段 5 收口与 N4 候选 | 最终一致性审计通过；公开已知问题，不以 hold/gap 阻断 V1；提交 N4 用户审核 | 生效至 N4 审核 |
| DEC-039 | N4 通过与 V1.0.0 正式发布 | USER 批准正式发布、`v1.0.0` 标签和 GitHub Release；已知问题继续公开 | 生效 |
| DEC-040 | V2 启动与 Charter 扩展 | USER 批准 V2-N1，并授权章程纳入 V2 治理、节点和权限；当前推进至 N2 原型审核 | 生效 |
| DEC-041 | V2-N2 静态优先原型与 Web Data 契约 | 静态优先、数据驱动、研究/策展分层；N2 通过前不扩展完整站点 | 生效 |
| DEC-042 | V2-N2 通过与阶段 5—7 解锁 | USER 同意继续执行完整数据、网站和 N3 测试站；N4 仍保留最终审核 | 生效 |
| DEC-043 | V2 完整测试站完成并提交 N3 | 完整测试站完成，N3 等待 USER 审核 | 历史节点，已由 DEC-044 收口 |
| DEC-044 | V2-N3 通过与 V2.0.0 发布候选准备 | USER 批准进入阶段 8；正式发布仍由 N4 决定 | 生效 |
| DEC-045 | Web 0.1.0 重基线与 Public Release 暂停 | V2 继续表示网站建设阶段；Web Product 重基线为 0.1.0；rc.1—rc.5 转为历史候选；正式发布改为未来 USER Gate | 生效，取代 DEC-044 的当前发布推进状态 |

## DEC-001：地域范围

决定：项目正式采用“拉丁美洲文学地图”，不是仅限南美洲。

备选项：

- **拉丁美洲：已选择。** 可以保留墨西哥、加勒比和拉美文学爆炸的完整脉络。
- 仅南美洲：未选择。范围更窄，但会排除墨西哥、加勒比等重要文学传统。

影响：项目名称、作家范围、地域模型和未来地图均按拉丁美洲设计。

## DEC-002：V1 成果

决定：V1 先完成内容数据库，不同时开发网站。

备选项：

- 研究展览网站：未选择。待内容数据库冻结后进入 V2。
- **内容数据库：已选择。** 优先完成资料、知识模型、研究文本和可追溯关系。
- 展示原型：未选择。不以牺牲内容质量换取早期视觉效果。

影响：V1 的主要验收对象是 SQLite、审核表、JSON、来源和质量报告。

## DEC-003：用户参与方式

当前决定：用户只做关键节点审核，其余工作由 AI 团队闭环推进。

历史选项：

- 节点确认：最初未选择，现经用户修订后采用。
- 仅最终验收：未选择，方向性返工风险过高。
- 逐阶段审核：曾选择，现已被本决策取代。

固定审核节点：

1. N1 范围基线；
2. N2 知识模型与试点；
3. N3 V1 候选版；
4. N4 正式发布。

影响：资料盘点、批次提取、交叉审核、补证、去重、导出和普通纠错均不再逐项请求用户确认。

## DEC-004：首版规模

决定：采用精品首版。

备选项：

- **精品首版：已选择。** 约 10 位作家、30 部作品、10 个主题和 150 条已核验关系。
- 标准首版：未选择。约 20 位作家、60 部作品和 300 条关系，审核与资料压力较大。
- 资料驱动：未选择。不完全随首批资料数量浮动，避免项目边界失控。

影响：V1 优先验证质量、可追溯性和关系密度，后续版本再扩容。

## DEC-005：资料边界

决定：采用开放研究扩展。

备选项：

- 书籍为主并补证：未选择。限制外部研究只做核验会压缩内容覆盖。
- 仅限提交资料：未选择。容易留下明显空缺。
- **开放研究扩展：已选择。** AI 可主动扩展学术与权威公共资料，但必须记录来源并接受证据审核。

影响：需要来源等级、访问日期、证据定位和冲突记录；普通网页不得作为解释性观点的唯一依据。

## DEC-006：数据交付

决定：采用三层数据包。

备选项：

- **三层数据包：已选择。** SQLite 主数据、Excel/CSV 审核层、JSON 网站层。
- 表格优先：未选择。后续建站仍需结构转换。
- JSON 优先：未选择。不利于用户和编辑人员抽样查看。

影响：SQLite 是唯一主数据，其他格式统一生成，禁止多份数据分别维护。

## DEC-007：策展主线

决定：V1 采用均衡文学地图，其他路线保留为后续专题。

备选项：

- **均衡文学地图：已选择。** 时间、地域、流派和主题均衡覆盖。
- 权力与正义：保留。后续可形成独裁、暴力、审判和记忆专题。
- 形式与先锋：保留。后续可形成时间、迷宫、叙事实验和文学爆炸专题。

影响：首版不被单一法律政治视角或形式主义视角支配，但数据模型必须能支持两类扩展。

## DEC-008：AI 与子 Agent 协同

决定：默认由一名项目经理 AI 协调最多三名并行 Worker/Reviewer，以多 Wave 方式推进。

规则：

- 共享 Schema 和主数据库串行修改；
- 研究 Agent 只写暂存区；
- Reviewer 不审核自己完成的批次；
- 数据整合由单一 Agent 完成；
- 节点之间允许自动派工、纠偏、补证、审核和合并；
- 只有方向性、敏感或不可逆问题才提前请求用户决定。

影响：减少用户微观审核和多 Agent 文件冲突，同时保留可追溯的过程状态。

## DEC-009：GitHub 与版本纪律

决定：建立公开 GitHub 仓库；每次完成独立修改并通过验证后提交和上传。

规则：

- 原始书籍、扫描件、版权全文、密钥和私有材料不得进入公开仓库；
- 每次发布同步更新本文件和 `CHANGELOG.md`；
- 方向性变更必须新增 DEC 编号或明确取代旧决策；
- 每个版本记录新增、修改、删除、决策变化、迁移、验证和未解决问题；
- 日常低风险修改由 AI 审核并上传，正式版本由用户在 N4 确认。

影响：GitHub 成为项目版本和公开成果的权威历史来源。

## DEC-010：AI 用量与跨平台分工

决定：将 OCR、资料清单、书目初填、Markdown 整理、机械核对和候选实体抽取优先交给其他 AI 软件；Codex 保留项目管理、Schema、证据判断、关系审核、主数据库、跨来源整合、质量门槛、版本和 GitHub 工作。

备选项：

- 全部由 Codex 完成：未选择。流程统一，但会把有限用量消耗在大量重复的 OCR 和整理工作上。
- **基础工作外部分流、Codex 负责核心收口：已选择。** 在控制用量的同时保留数据结构和研究判断的一致性。
- 全部交给其他 AI：未选择。不同平台的字段、来源和质量标准容易漂移，且主数据缺少统一责任人。

交接规则：

- 所有外部 AI 任务必须绑定 `TASKS.md` 中的任务或由 Codex 新建任务卡；
- 外部 AI 不直接修改 `TASKS.md`、主数据库、决策记录或 GitHub；
- 每个任务包必须包含任务说明、状态、主体成果、QA、问题清单、文件清单和 HANDOFF；
- 外部 AI 的实体和关系结果默认是候选，必须经过复核与整合后才能进入主数据；
- Reviewer 不审核自己的任务；
- 原始书籍和整书 OCR 只在本地或用户授权的私有空间流转。

影响：阶段 1 的资料盘点与 OCR 可以在 N1 后主要由其他 AI 推进；Codex 用量集中在派单、抽检、模型设计、证据审核、整合和节点交付。

## DEC-011：N1-OCR-001 R2 验收与先行证据登记

决定：`N1-OCR-001` R2 在既定 6 份资料、4 本 PDF 仅 L1 的范围内通过 Codex 独立复检，作为 `V1-S1-001/003/004/005` 的先行交付证据登记。

边界：

- 本决定不取代 `V1-N1-001` 用户节点审核，也不改变阶段 1 任务的 `blocked` 状态；
- 在 N1 通过前不分配正式 `SRC-`，候选实体与关系不进入 `data/staging`；
- 西语目录候选继续保持 `medium`，只有逐字目录事实在进入暂存前逐条复核后才能单独升为 `high`；
- 解释性关系仍需直接证据或第二来源，不能因 OCR 返修通过自动升为已确认事实；
- WEB-003 保持 B，网络来源的正式编号在后续入库任务统一分配；
- 原件、`inputs/`、完整 EPUB 提取件和整书 OCR 继续 `local_only`，不得进入公开 GitHub 仓库。

影响：项目获得第一批可复用的阶段 1 先行证据，同时保持用户节点审核、正式编号、暂存和公开边界不被绕过。

## DEC-012：N1 范围基线正式通过

决定：用户于 2026-08-04 批准 `docs/N1_范围基线审核包.md` 的建议范围，阶段 1 正式解锁。

影响：资料接收、来源建档、L1 OCR、机械复核和首批汇总可以由 AI 团队按依赖自动推进，无需逐项请求用户审核。N1 不授权公开原书或整书 OCR，不冻结 Schema，不执行不可逆主数据迁移，也不构成正式发布批准。

## DEC-013：首批六份资料的正式来源编号

决定：按首批任务包的稳定接收顺序，将 `TMP-001`~`TMP-006` 映射为 `SRC-0001`~`SRC-0006`。不同数字版本继续分别编号，未来新增来源从 `SRC-0007` 起顺延。

执行边界：正式 ID 由 Codex 分配；外部 AI 只执行机械回填。回填必须保留临时 ID 映射，不得修改 OCR 正文、候选 ID、locator、证据、置信度或关系语义。回填结果由独立外部 Reviewer 复核后，才由 Codex决定是否进入 `data/staging`。

影响：六份已验收资料获得稳定引用标识，为阶段 1 汇总和阶段 2 试点选择提供前提。

## DEC-014：建立冻结的项目总章程

决定：在仓库根目录建立 `PROJECT_CHARTER.md`，集中规定项目使命、V1/V2/V3 路线、内容规模、完整阶段流程、角色权限、数据生命周期、质量门槛、最终成果和章程变更程序。

治理规则：该文件是项目最高规范和长期目标锚点。未经用户明确、具体授权，任何 AI、Agent、脚本、外部 Worker 或 Reviewer 均不得修改、移动、重命名或删除。一般性的“继续推进”“更新文档”“按计划执行”不构成修改授权。

影响：`TASKS.md` 继续作为唯一动态状态源；原阶段 0 章程、AI 协同计划、研究规范、外部 AI 手册和具体任务卡均为总章程的下位文件。后续 AI 必须先读总章程，再领取任务。

## DEC-015：GitHub 批量上传与用户触发

决定：取代 DEC-009 中“每个独立修改完成后自动提交上传”的规则。通过审计的本地变更可以累积；仅当用户明确通知“上传”或“提交并上传”时，Codex 才执行公开文件的批量 Git 提交和 GitHub 上传。

保持不变：上传前仍须审计公开边界，不得包含原书、整书 OCR、私人资料、密钥或未授权的长摘录；每个上传批次仍必须同步更新 README、决策记录和 CHANGELOG。

影响：无数据迁移或返工。已通过的任务仍按 `TASKS.md` 更新；对外公开收口改为由用户触发的批量操作。

状态：本决策已由 DEC-031 取代。其“不过度按小任务提交”和公开边界规则继续保留，逐次用户触发规则停止适用。

## DEC-016：阶段 2 试点作品、来源等级与获取策略

决定：阶段 2 采用博尔赫斯《阿莱夫》《小径分岔的花园》《虚构集》和李斯佩克朵《活水》《星辰时刻》《家庭纽带》，共六部作品。首轮使用 SRC-0002 及七条合法开放候选来源，先做访问与材料覆盖检查，再派窄范围 L2。

等级核定：六条学术论文/学位论文候选核定为 A；十二条图书馆、出版社、基金会、大学研究中心或同等级权威机构来源核定为 B；文汇报出版报道核定为 C；两条 ISFDB 社区书目记录降为 D，只作发现线索。

获取策略：本轮不购买 JSTOR 全文或中文版全集，不预约 FCRB 档案；9 条葡语候选和 7 个巴西机构视为满足阶段 2 补强要求，但不代表 V1 葡语覆盖永久完成。

影响：`V1-S2-001/002` 完成，`V1-S2-003-A` 解锁。材料检查通过前不为新增网络来源分配正式 `SRC-`，不下载或搬运完整受版权保护内容。详细页码定位要求随后由 DEC-017 调整。

## DEC-017：来源级证据定位

决定：经用户明确授权，将 V1 的最低证据定位从“书籍章节/页码/电子位置”调整为“具体来源”。书籍记录来源 ID 和书名；论文记录来源 ID、论文题名及 DOI/稳定 URL（如有）；网页记录页面标题、机构/作者、URL 和访问日期。章节、页码、电子位置、段落锚点和短摘录均为可选增强信息。

保持不变：来源优先、候选与事实分离、解释性关系原则上双来源、来源等级、Reviewer 独立、原件不变和最小公开规则继续生效。

迁移：不删除历史页码和 locator；已通过任务不返工。未来候选与关系准入只强制有效来源 ID 和来源题名，已有详细定位继续保留。`V1-S2-003-A` 已生成的页段表作为增强成果使用，不要求补齐待核页码。

影响：总章程升级至 1.2.0；研究规范、任务源、协同计划、外部 AI 手册、模板和阶段 2 活跃任务同步更新。

## DEC-018：双 Agent 单任务队列与交付精简

决定：在当前只有 Codex 和 WorkBuddy 的条件下，采用双 Agent 单任务队列。机械任务使用 `LITE` 包，OCR、研究、关系和数据库任务使用 `FULL` 包；返修采用差量模式；Worker 先运行机械检查；聊天提示词只作短启动器。

对 DEC-008 的调整：保留未来多 Agent Wave 架构，但当前不制造名义上的 Reviewer/Worker 并行。Codex负责独立验收，WorkBuddy 不审核自己的成果；同一时间只给 WorkBuddy 一个执行任务。

状态与版本：`TASKS.md` 是唯一实时状态源。README、决策记录和 CHANGELOG 在里程碑批量同步；本决策形成时采用逐次用户通知上传，该时机规则现已由 DEC-031 的节点式自动发布取代。

影响：减少重复过程文档、聊天上下文和多文件数字同步，目标是把正常外部任务控制为一次初审、最多一次窄范围返修。

## DEC-019：里程碑任务包与关键门禁审核

决定：保持 Codex + WorkBuddy 单队列和文件写入边界，但不再把连续机械工作拆成大量小卡。每张 WorkBuddy 任务卡原则上合并 2—4 个相邻、依赖连续且可由同一输入完成的子任务，统一生成一个里程碑交接包。Codex 不逐页、逐条重做 Worker 的工作，只在关键门禁抽核。

四个默认门禁：

1. 文件结构、CSV 可解析性、ID/引用完整性和公开安全；
2. 来源身份、合法访问、范围与覆盖；
3. 语义抽样、候选与事实边界、冲突处置；
4. `data/staging`、主数据库、版本或发布准入。

执行规则：Worker 必须运行项目共享验证脚本并把结果写入 HANDOFF/QA；非阻塞的页码、格式增强和可选定位不触发返修；机械门禁失败只做差量修复；只有抽样失败、来源错配、语义越权或数据断裂时才扩大复核范围。

阶段 2 影响：`V1-S2-003-A` R1 通过并完成正式来源编号后，来源级 L2 文本整理与 `V1-S2-004` 机械候选抽取合并为一个 WorkBuddy 里程碑包。Schema、关系确认、暂存和数据库仍由 Codex 负责。

章程影响：本决策只调整任务层执行粒度和审核方式，不改变 `PROJECT_CHARTER.md` 的使命、成果、角色边界或用户节点，因此不修改冻结总章程。

## DEC-020：阶段 2 七个网络来源正式编号

决定：`V1-S2-003-A` R1 通过来源身份、合法访问、作品覆盖和机械门禁后，按候选 ID 顺序分配：`DISC-BOR-002`→`SRC-0007`、`DISC-BOR-004`→`SRC-0008`、`DISC-LIS-003`→`SRC-0009`、`DISC-LIS-004`→`SRC-0010`、`DISC-LIS-005`→`SRC-0011`、`DISC-LIS-009`→`SRC-0012`、`DISC-LIS-010`→`SRC-0013`。

登记规则：正式登记表增加 `persistent_id` 与 `canonical_url` 两列，以满足论文 DOI/卷期标识和网页稳定 URL 的来源级证据要求；既有 `SRC-0001`~`SRC-0006` 的内容和编号不变。

影响：后续候选成果只能使用正式 `SRC-0002`、`SRC-0007`~`SRC-0013`；legacy `DISC-` ID 只保留在映射与历史交付中。正式来源编号不代表候选实体已通过语义审核，也不自动进入暂存或主数据库。

## DEC-021：更换后续外部执行方

决定：自 2026-08-09 起，不再向 WorkBuddy 派发新任务；尚未开始的 `V1-S2-003-004-B` 改派给新外部执行方，临时代号 `EXT-AI-02`。新执行方必须在首次交付中登记真实平台、供应方和模型名称，之后由 Codex把临时代号正常化。

历史与署名：WorkBuddy 已完成的任务包、返修记录和验收结论保持只读、保留原署名；新执行方不得覆盖旧交付、修改 REVIEW 或把历史成果归为己有。

协作方式：仍采用里程碑大任务包和 Codex四类关键门禁，不因供应方变化恢复逐页、逐项审核。新执行方只能执行 `TASKS.md` 已解锁且由用户/Codex明确发送的任务，不自行领取后续阶段。

章程与权限：供应方更换不改变总章程、角色分工、证据标准、用户节点、GitHub 上传规则或版权边界，因此不修改冻结的 `PROJECT_CHARTER.md`。

## DEC-022：阶段 2 候选层通过与试行实体分层

决定：`V1-S2-003-004-B` R0 通过关键门禁。EXT-AI-02（ZCode / deepseek-v4-flash，精确版本 unknown）交付的八来源笔记、六作品摘要和 122 条实体候选可作为 Schema 设计输入，但仍停留在候选层，不自动进入 `data/staging`。

实体分层：试行 Schema 至少区分 `work`（单篇作品）、`collection`（作品集）、`edition`（版次/译本）和 `adaptation`（改编），并支持 `CONTAINS_WORK`、`TRANSLATION_OF`、`ADAPTATION_OF`。短篇《阿莱夫》与 1949 年同名作品集必须分别建模；论文中出现的作品名作为来源证据归并，不另建文学作品；英译本和电影改编分别按版次/译本与改编处理。

去重：重复作者和同一作品候选归并到规范实体，但保留多来源证据。`vanguardismo hispanoamericano` 不机械并入泛称“先锋派”；卢奇安《真实的故事》与《征服新西班牙信史》禁止按标题子串合并。

书目核定：李斯佩克朵生卒年采用 1920—1977；`El tamaño de mi esperanza` 规范出版年采用 1926，SRC-0002 的 1925 保留为来源冲突；UESB 论文页码在无法从官方落地页确认时允许留空。SRC-0007 只作 B 级题名/书目交叉证据；SRC-0008 官方论文按全文研究来源使用。

影响：`V1-S2-003/004` 完成，`V1-S2-005` 解锁。关系抽取、暂存准入和数据库生成继续等待 Schema 与迁移规则完成。本决策不修改总章程，也不触发 GitHub 上传。

## DEC-023：N2 前试行 Schema 与关系候选派发

决定：采用 `docs/阶段2_试行Schema与迁移规则.md` 的 0.1 草案作为 `V1-S2-006-A` 唯一数据结构依据。Schema 区分 author、work、collection、edition、adaptation 等实体层，并暂时允许 CREATED、CONTAINS_WORK、EDITION_OF、TRANSLATION_OF、ADAPTED_FROM、SET_IN、ASSOCIATED_WITH_PLACE、ASSOCIATED_WITH_MOVEMENT、EXPLORES_THEME、RESPONDS_TO_WORK、INFLUENCED_BY 共 11 个关系词。

证据：每条候选必须关联来源 ID 和来源题名；解释性关系不得由标题、关键词或常识推断。相同三元关系的不同来源分行保存并共享 `relation_group_id`；只有单一来源时标 `needs_second_source`，不得直接视为已确认关系。

派发：`V1-S2-006-A` 交给 EXT-AI-02，一次生成 20—30 条关系候选、30—50 条原子事实候选、两位作家与六部作品内容卡草稿及证据覆盖表。外部 AI 不拥有 Schema 修改、关系核定、暂存或主数据库权限。

影响：`V1-S2-005` 完成，`V1-S2-006` 解锁。试行 Schema 在 N2 用户节点前仍可由 Codex作兼容调整，正式冻结必须经过 `V1-N2-001`。本决策不触发 Git 或 GitHub 上传。

## DEC-024：阶段 2 关系候选与事实素材准入

决定：`V1-S2-006-A` R1 通过关键门禁。27 条关系候选（26 组）、49 条事实候选、8 张内容卡草稿和证据覆盖表可作为试点数据包输入；其中 11 条单来源解释关系继续标 `needs_second_source`，不得与状态为 `none` 的关系混为已确认事实。

清理结果：导演与电影不得借用 `CREATED`；英文跨时期选集不得仅凭题名与 1949 年作品集建立 `TRANSLATION_OF`；复合主题只有在来源支持完全相同三元组时才可合并为双来源组。本轮唯一双来源解释关系为《星辰时刻》与“社会不平等及书写”。

显示与补证：同名原著、改编、短篇和作品集在暂存层使用带年份和层级的显示名。`CONTAINS_WORK`、两篇短篇年份和李斯佩克朵卒年由 Codex在来源登记/暂存整合时按需补充；无证据的 `RESPONDS_TO_WORK` 继续为空。

影响：`V1-S2-006` 完成，`V1-S2-007` 解锁并由 Codex执行 SQLite/CSV/JSON 试点包。N2 前不把单来源解释关系提升为已确认关系。本决策不修改总章程，也不触发 GitHub 上传。

## DEC-025：试点数据包 PRE 层预组装

决定：在 `V1-S2-007` 内增加外部支持任务 `V1-S2-007-A`。EXT-AI-02 可把已经通过门禁的 28 个关系端点、26 个关系组、27 条证据、49 条事实、8 张内容卡和 8 个来源机械生成 CSV、JSON、SQLite 三格式预览。

ID 与权限：外部交付只能使用 `PRE-` 临时预览 ID；不得分配正式实体、关系或 staging ID，不得修改 `data/`。关系只分为 `eligible_for_staging_review` 与 `hold_needs_second_source`，不得使用 confirmed/approved 等正式准入表述。

一致性：JSON 与 SQLite 必须由同一组六张 CSV 通过可重复脚本生成，并提供逐表行数、主键、SHA-256、外键和 SQLite 完整性报告。Codex只在这些机械门禁通过后执行正式映射、最终数据包和 N2 审核材料。

影响：提高外部 AI 的机械处理占比，但不转移 Schema、语义、正式暂存、数据库定稿或用户节点决策权。本决策不修改总章程，也不触发 GitHub 上传。

## DEC-026：PRE 数据包准入与正式整合口径

决定：`V1-S2-007-A` R0 通过数据包门禁。28 个 PRE 实体、26 个关系组、27 条证据、49 条事实、8 张草稿卡片和 8 个来源的 CSV/JSON/SQLite 逻辑内容一致，可作为 Codex 正式暂存与 N2 包的输入。15 个 `eligible_for_staging_review` 进入正式审核，11 个 `hold_needs_second_source` 继续留在候选层。

可重复构建：跨环境验收以 JSON、逐表行数、主键集合、规范化内容 SHA-256、SQLite 完整性和外键结果为准，不要求不同 SQLite 库版本生成的数据库文件原始 SHA-256 相同。最终构建必须记录 SQLite 库版本。

实体与关系：1949 年作品集《阿莱夫》在 N2 包中单独建立第 9 张结构型简卡，不与 1945 年短篇合并。`DIRECTED` 作为 N2 关系词提案；英文选集 0072 按 edition/anthology 候选处理，不恢复无直接证据的 `TRANSLATION_OF`。`CONTAINS_WORK` 只在权威目录直接支持时补入，`RESPONDS_TO_WORK` 无证据时继续为空。

来源与数据库：短篇年份和李斯佩克朵卒年由 Codex 用权威机构来源补入。最终 SQLite 使用关系来源、事实来源、卡片事实等关联表表达多值关系，避免把分号字符串作为主数据库结构；CSV/JSON 仍可提供扁平化导出。

影响：`V1-S2-007-A` 完成，父任务继续由 Codex 收口。N2 用户审核仍是 Schema 冻结前的必经节点。本决定不修改总章程，也不触发 GitHub 上传。

## DEC-027：N2 正式暂存包与用户审核提案

决定：Codex 已完成 `V1-S2-007` 正式暂存收口，生成 28 个 STG 实体、15 个 `accepted_for_n2` 关系、11 个独立 hold 关系、52 条来源事实、9 张内容卡和 11 个来源的 CSV/JSON/SQLite 同源数据包。新增 SRC-0014~0016，分别支持《小径分岔的花园》1941、短篇《阿莱夫》1945 和李斯佩克朵卒年 1977。

N2 提案：维持 work/collection/edition/adaptation 分层；把 `DIRECTED` 加入正式关系词；不另设与 `CONTAINS_WORK` 同义的关系词；直接事实允许一个合格来源，解释性关系原则上需要两个独立来源；内容卡采用事实清楚、篇幅克制的中文资料卡；继续采用来源级定位和可选页码。

状态：本决定的数据包生成与门禁部分已执行；Schema 冻结、`DIRECTED` 正式生效和阶段 3 解锁仍等待用户在 `V1-N2-001` 确认。

影响：阶段 2 数据任务完成，用户无需审核逐行事实，只审核四项模型方向。本决定不修改总章程，也不触发 GitHub 上传。

## DEC-028：N2 通过、Schema 0.2 冻结与阶段 3 批次

决定：用户于 2026-08-10 按建议通过 N2。V1 Schema 0.2 正式冻结，保持 work/collection/edition/adaptation 分层，增加非作家相关人物 `person` 支撑导演等角色，正式加入 `DIRECTED`，不增加与 `CONTAINS_WORK` 同义的关系词。直接事实允许一个合格来源；解释性关系原则上需要两个独立来源，单来源继续 hold。内容卡采用事实清楚、篇幅克制的中文资料卡；来源级定位继续生效，页码可选。

阶段 3：剩余 8 位核心作家分为三个串行里程碑包。B01 为埃莱娜·加罗、罗萨里奥·卡斯特利亚诺斯、胡安·鲁尔福，优先解决女性作家和墨西哥覆盖；B02 为加西亚·马尔克斯、胡利奥·科塔萨尔、阿莱霍·卡彭铁尔；B03 为马里奥·巴尔加斯·略萨、巴勃罗·聂鲁达。每位固定 3 部作品，共新增 24 部，与阶段 2 的 6 部合计 30 部。

执行：每包由 EXT-AI-02 一次完成来源、候选实体、事实、关系、内容卡素材、查重和覆盖汇总；Codex只做关键门禁并负责暂存整合。B01 通过前不启动 B02，避免共享数据和研究口径漂移。

影响：`V1-N2-001`、`V1-S3-001` 完成，`V1-S3-B01` 解锁。本决定不修改总章程，也不触发 GitHub 上传。

## DEC-029：B01 墨西哥三作家批次准入

决定：`V1-S3-B01` R0 通过关键门禁。接受 15 个 A/B 来源、44 个实体候选、81 条事实候选、31 个关系组和12张内容卡草稿；其中24个关系进入 Codex 暂存审核，7个单来源解释关系继续 hold。

规范化：胡安·鲁尔福、《佩德罗·巴拉莫》作品、墨西哥、奥克塔维奥·帕斯、墨西哥革命小说与既有同类候选归并；主题“记忆与遗忘”归入 `STG-ENT-0014`。人物“佩德罗·巴拉莫（人物）”与同名作品保持独立，不因名称相同合并。

来源：接受 UNAM、智利大学、ELEM、FCE 对被拦截来源的替代。本批不为四部只有 B 级直接事实来源的作品单独派专论补证；只有新增解释性关系时才在阶段4择优补充。不得绕过原站访问限制。

影响：B01 完成，B02 解锁；正式 SRC 编号和暂存映射由 Codex继续执行。本决定不修改总章程，也不触发 GitHub 上传。

## DEC-030：B02 文学爆炸与加勒比三作家批次准入

决定：`V1-S3-B02` R1 通过关键门禁。接受 15 个来源候选、43 个实体候选、68 条事实候选、34 个关系组和 12 张内容卡草稿；其中 19 个关系进入后续暂存审核，15 个单来源解释关系继续 hold。候选包通过不等于自动写入主数据库。

补证：R1 已删除无直接来源的生卒年、首发年、国籍与马孔多场景事实。阶段 4 统一派一个权威基础事实补核包；在取得直接来源前，实体候选表中标明“待核验”的日期或国家值不得迁入暂存事实。

规范化：`新巴洛克（neobarroco）` 与范围更广的 `巴洛克（barroco）` 保持独立；没有直接来源时不建立同义、从属或影响关系。人物 `上校（《没有人给他写信的上校》人物）` 与同名作品保持分层，不合并。

改编链：非固定九部作品的短篇—电影—导演链保留为补充候选。阶段 4 只有决定纳入改编专题时，才先补作品集 `Todos los fuegos el fuego` 和有直接来源的 `CONTAINS_WORK`，再整体迁移；否则继续归档为补充候选。

影响：B02 完成，B03 解锁；正式 SRC 编号、跨批次合并和暂存映射仍由 Codex执行。本决定不修改总章程，也不触发 GitHub 上传。

## DEC-031：V1 节点式 GitHub 自动发布

用户授权：USER 于 2026-08-10 明确批准修改 `PROJECT_CHARTER.md`，同意按 `docs/章程修改提案_2026-08-10_V1节点式GitHub自动发布.md` 执行，并指定从 `GIT-V1-S3` 开始生效。

决定：取代 DEC-015 的“每次上传另行通知”规则。普通任务、返修和局部补证继续在本地累积；达到以下节点且全部门禁满足后，Codex 无需再次请求上传指令，自动审计、提交并推送可公开成果：

1. `GIT-V1-S3`：B01—B03 全部通过，提交阶段 3 及存量 N2 公开成果，标签 `v1-s3`；
2. `GIT-V1-N3`：USER 通过候选版，提交候选数据包，标签 `v1-n3`；
3. `GIT-V1-N4`：USER 通过正式发布，提交 V1 正式包，标签 `v1.0.0` 并创建 GitHub Release。

安全边界：按文件名逐项暂存，不使用 `git add .`；不得上传原书、整书 OCR、论文全文、`inputs/`、密钥、Cookie、环境配置、缓存或 `local_only` 材料；禁止 force push、历史改写、删除远端对象和绕过 hooks。节点、公开边界、验证、远端或权限状态不明时 fail-closed 停止并报告 USER。

影响：章程升级至 1.3.0；不改变 V1 范围、Schema、用户 N1—N4 审核权或外部 AI 权限。当前 B03 工作不中断，下一次自动 GitHub 动作为 B03 通过且阶段 3 汇总完成后的 `GIT-V1-S3`。

## DEC-032：B03 准入、阶段 3 收口与阶段 4 补证条件

决定：`V1-S3-B03` R1 通过关键门禁。接受 12 个来源、31 个实体候选、59 条事实、23 个关系组和 8 张内容卡草稿；16 个关系组进入阶段 4 暂存审核，7 个单来源解释关系继续 hold。阶段 3 三批合计 42 来源、118 实体候选、208 事实、88 个关系组和 32 张卡片；59 个关系组 eligible，29 个 hold。

规范化：三部聂鲁达诗集按 `collection` 合并，单篇诗继续独立为 `work`；同名或旧候选中的 `work` 类型不直接继承。未经直接来源确认的 Antônio Conselheiro 别名不得迁入暂存事实或正式别名表。

补证：B01—B03 遗留的生卒年、首发年、诗集分卷和历史事件基础值合并为阶段 4 一个权威补证包，不逐项派小任务。外部 AI 同包整理 `BASED_ON_EVENT` 的证据与兼容性影响，但不得自行修改 Schema 0.2；端点、迁移、受控词表和是否提交用户节点由 Codex 决定。

政治诗歌：作品级主题必须有明确指向具体作品或诗集的来源。单一合格来源保持 hold，两个独立合格来源才可建议 eligible；只描述作者总体风格或政治身份的材料不得自动下推到其全部作品。

影响：阶段 3 完成，阶段 4 和 `V1-S4-A01` 解锁；`GIT-V1-S3` 自动发布门禁满足。本决定不直接写主数据库，也不修改总章程或冻结 Schema。

## DEC-033：A03 正式来源编号、关系词与争议收口

决定：`V1-S4-A02` R1 通过后，由 Codex按已批准的 `proposed_order` 为 56 条新来源连续分配 `SRC-0017`~`SRC-0072`。A01-SRC-0003 复用 B03-SRC-0003 的 `SRC-0046`，A01-SRC-0004 复用 B02-SRC-0012 的 `SRC-0042`；三条 Dialnet 查询页继续 hold，不分配正式来源 ID。正式注册表共 72 行，候选映射共 61 行。

关系词：冻结 Schema 0.2 的 12 个关系词全部保留原名、端点和证据口径，不增加同义词。阶段 3 的 88 个关系组仍为 eligible×59、hold×29；A01/A02 新证据没有为 29 个单来源解释关系中的任何同一三元组提供第二独立来源，故 29 个全部继续 hold。A01-SRC-0002 的直接证据恢复一个 `《百年孤独》 SET_IN 马孔多` 候选，进入暂存审核但不自动写入 staging。

事实与诗歌：A01/A02 共形成 22 条事实准入裁决，其中 20 条进入暂存审核、`《消逝的足迹》1953` 继续 hold、el Consejero/Antônio Conselheiro 只作人物原型建模说明。《上校》拆为 1958 年杂志刊载与 1961 年书版两个字段。政治诗歌维持 hold×3、作者级不下推×1、gap×1。

事件兼容性：`BASED_ON_EVENT` 只形成 N3 提案，端点限定 `work → event`。当前为直接证据 1、间接证据 2、独立来源 2；同源标题证据不重复计数。用户在 N3 批准前不修改 Schema 0.2、不生成正式关系；若批准，采用加法兼容升级，现有 12 个关系词和既有数据不迁移。

影响：`V1-S4-A03` 完成，`V1-S4-A04` 解锁。下一包由外部 AI 整理 10 位作家、30 部作品的事实卡/内容卡素材并做策展覆盖审计；用户仍只在 N3 审核方向、覆盖、重大争议和缺口。本决定不修改冻结总章程，不触发 Git/GitHub。

## DEC-034：A04 收口与 A05 有边界差量补包

决定：`V1-S4-A04` R1 通过关键门禁。接受 40 张目标卡、238 条事实素材、77 条卡片来源矩阵和 22 条覆盖审计；非准入材料已与正式事实分栏，单来源解释已改为来源归属式展示，《金鸡》拆为 1958 写作年与 1980 出版年。当前审核关系仍为 eligible/accepted×75、hold×40，S1 legacy 117 另列，章程 150 条门槛的真实差距仍为 75。

关系规模：不调整冻结章程门槛，不把 legacy 原始行整批平移。`V1-S4-A05` 对 117 条 legacy 逐行核对端点、Schema 0.2 关系词、来源、证据等级、与当前 75 条的重复及 V1 范围；只有去重后的净新增 eligible 候选才进入 Codex 门禁。解释性单来源继续 hold，Schema 不兼容、端点未解析和范围外记录不得计数。

差量补证：A05 同包对 7 部 research_gap 作品各核查最多 1—2 个合法开放 A 级研究入口，并对《消逝的足迹》1953 最多核查 2 个开放权威书目入口。找不到即保持 gap/hold，不购买、不订阅、不绕过。外部 AI 只生成候选，不分配正式 ID、不改 Schema、不写 staging。

时序：《上校》1958 杂志刊载/1961 书版双字段在 A05 通过后的 A06 候选包组装中由 Codex 写入暂存候选；《消逝的足迹》1953 未获权威交叉前继续 hold。政治诗歌 3/1/1 与 `BASED_ON_EVENT` 分别作为展示边界卡和 Schema 兼容性卡编入 N3，用户节点批准前不生成正式事件关系。

影响：A04 与并入的策展覆盖审计完成，A05 解锁，A06 继续锁定至 A05 通过。用户仍只在 N3 审核方向、覆盖、重大争议和事件关系提案。本决定不修改冻结总章程或 Schema，不触发 Git/GitHub。

## DEC-035：A05 收口、缺口止损与 A06 PRE 预组装

决定：`V1-S4-A05` R1 通过关键门禁。117 行 S1 legacy 的可逆审计得到 9 个重复组、6 个解释性单来源 hold、9 行 Schema 不兼容、15 行端点未解析和 76 行范围外记录；净新增 eligible 为 0，V1 主关系候选保持 75，章程 150 条门槛真实差距保持 75。9 个重复组的 11 行 legacy 证据可在 A06 作为既有关系的附加证据建议，但不得增加关系数。

研究缺口：7 部作品补核最终只有《金鸡》取得作品级 A 级研究入口；《彩色的一周》只有作品集内同名短篇的局部支持，其余 5 部为 not_found。《消逝的足迹》1953 的两次权威目录尝试仍未形成可直接核验记录。本阶段不重置访问预算、不再追加研究轮次；所有未关闭项如实进入 N3 缺口说明。

来源：A05-SRC-0001 保留为 A 级短篇局部研究候选，A05-SRC-0003 保留为 A 级作品级来源候选；正式编号留给 Codex。A05-SRC-0002 只作 C 级会议摘要发现线索，不进入正式编号队列。

A06 分工：增加外部支持任务 `V1-S4-A06-A`，由 EXT-AI-02 把既有通过成果机械预组装为 PRE CSV/JSON 和 N3 材料；不得研究、分配正式 ID、合并实体、修改 Schema 或写入 `data/`。通过 Codex 门禁后，父任务 `V1-S4-006` 再由 CODEX-DATA 生成正式 SQLite、Excel/CSV、JSON、数据字典、来源目录和质量报告。

N3：用户节点分别决定 75/150 门槛、work→event 的 `BASED_ON_EVENT`、4 行 author→event 的独立兼容性问题，以及政治诗歌 3/1/1 的证据与展示边界。N3 前不修改 Schema 0.2、不生成事件正式关系、不以 legacy 或 hold 补数。

影响：A05 完成，A06-A 解锁，正式 V1 候选包继续等待 A06-A 门禁。本决定不修改冻结总章程，不触发 `GIT-V1-N3`。

## DEC-036：A06 正式候选包、来源编号与 N3 门禁

门禁：`V1-S4-A06-A` R1 通过。R1 只修正 STATUS、QA_REPORT、ISSUES 和 MANIFEST；11 张 CSV、同源 JSON、构建脚本、40 卡文稿、N3 材料及其规范化哈希与 R0 保持不变。事实分层为 accepted_for_n2×50、batch_retained_candidate×161、candidate_for_staging_review×20、hold×2、research_note_only×1、gap×2、not_work_level×1、pending_n3×1，共 238；237 条有正式来源，唯一无来源为 A04-MAT-0235。

来源：A05-SRC-0001 分配 `SRC-0073`，只作为《彩色的一周》作品集内同名短篇的 A 级局部研究支持，不关闭作品集 research_gap；A05-SRC-0003 分配 `SRC-0074`，作为《金鸡》的 A 级作品级研究来源。A05-SRC-0002 继续作为 C 级会议摘要发现线索，不进入正式编号队列。正式来源连续扩展至 `SRC-0074`。

实体与关系：146 个 PRE 实体引用经已批准的 exact 规范化形成 144 个 `V1-ENT` 候选；阿根廷和“记忆与遗忘”分别复用既有同实体，其他引用保持分层。B03-ENT-0028 el Consejero 不与 Antônio Conselheiro 合并。关系保持 75 个可审核候选和 40 个 hold；11 行 legacy 证据回接 9 个既有关系，关系增量为 0。冻结 Schema 0.2 的 12 个关系词不变，`BASED_ON_EVENT` 和 author→event 均未进入关系表。

数据包：Codex 生成 `data/staging/v1_candidate/`，包含 17 张正式 CSV、同源 JSON、具外键的 SQLite、18 工作表 Excel、数据字典、来源目录、质量报告、Excel QA、N3 审核包和 MANIFEST。独立验证为 0 错误/0 警告；SQLite integrity_check 为 ok、foreign_key_check 为 0；Excel 公式错误为 0。该目录是 N3 前正式候选快照，不是主数据库冻结或用户批准。

N3：USER 只决定四项方向性问题：75/150 门槛口径、work→event 的 `BASED_ON_EVENT` 加法兼容、4 行 author→event 是否继续只作说明，以及政治诗歌 3/1/1 的展示边界。用户作出决定前不修改冻结章程或 Schema，不生成事件正式关系，不执行 `GIT-V1-N3`。

影响：`V1-S4-A06-A` 与父任务 `V1-S4-006` 完成，`V1-N3-001` 进入 review。本决定不修改 `PROJECT_CHARTER.md` 或 Schema 0.2，不提交、不推送、不创建标签。

## DEC-037：N3 通过、关系门槛调整与 Schema 0.3

用户授权：USER 于 2026-08-11 回复“全部按 Codex 建议：B/A/A/A”，并明确批准按 N3-DEC-001 修改 `PROJECT_CHARTER.md`、按 N3-DEC-002 对 Schema 作加法兼容升级。

门槛：V1 最低关系门槛由 150 条调整为 75 条经审核、有来源依据的关系；150 条转为 V1.1 扩展目标。hold、legacy 原始行和 pending 候选不得用于补数。章程升级至 1.4.0。

事件关系：Schema 由 0.2 升级为 0.3，新增第 13 个关系词 `BASED_ON_EVENT`，端点严格限定 `work → event`。根据 A01 的直接证据 1、间接证据 2、独立来源 2，生成 `V1-REL-0076`：《世界末日之战》→卡努杜斯战争；其余 12 个关系词和既有数据不迁移。

边界：4 行 author→event 不设计新关系词，只作事件说明；政治诗歌维持 hold×3、作者级×1、gap×1，不把作者级材料下推到具体作品。40 个关系 hold 保持原状态。

数据：N3 后候选包为 74 来源、144 实体、76 可审核关系、40 hold、91 条关系证据、238 事实和 40 卡片。CSV、JSON、SQLite、Excel 同源重建并通过完整性、外键、公式和引用检查。

影响：`V1-N3-001` 与 `V1-S5-001` 完成，解锁 `GIT-V1-N3` 和阶段 5。N4 正式发布权仍保留给 USER。

## DEC-038：阶段 5 收口、已知问题披露与 N4 候选

决定：N3 后候选包通过最终一致性和引用审计。CSV、JSON、SQLite、Excel 的表计数、主键和内容一致；SQLite 完整性为 ok、外键错误为 0；Excel 18 个工作表公式错误为 0；来源、实体、关系、事实和卡片引用零悬空。

披露：40 个单来源解释关系继续 hold；research_gap 为 verified×1、partial×1、not_found×5；《消逝的足迹》1953 继续 hold；政治诗歌维持 3/1/1；author→event 只作说明；`V1-FCT-0235` 是无来源的缺口说明而非事实。这些项目公开列入 `docs/V1_已知问题.md`，不因版本发布而隐去或确定化。

发布候选：V1 内容数据库的 N4 候选版包含 74 来源、144 实体、76 条经审核关系、40 hold、238 条事实素材和 40 张内容卡。网站、小程序、地图和展览交互不属于 V1。

Git 状态：`GIT-V1-N3` 内容提交 `5b1cec7` 已经 PR #2 合并到 `origin/main`，合并提交为 `1f2ad54`，`v1-n3` 标签已创建并推送。

影响：`V1-S5-002~004` 完成，`V1-N4-001` 进入 review；随后由 DEC-039 收口。

## DEC-039：N4 通过与 V1.0.0 正式发布

用户授权：USER 于 2026-08-11 明确批准将公开治理文档、来源目录、结构化候选数据，以及 `data/staging/v1_candidate/` 内的 CSV、JSON、SQLite、Excel、质量报告和相关构建验证脚本推送至指定 GitHub 仓库，并批准更新 `origin/main`、创建 `v1-n3`；同时批准 V1-N4 正式发布、`v1.0.0` 标签和 GitHub Release。

发布决定：以通过阶段 5 最终审计的同源数据包作为 V1.0.0 正式内容数据库。发布范围为 74 项正式来源、144 个规范实体、76 条经审核关系、40 条 hold、238 条事实素材和 40 张内容卡；CSV、JSON、SQLite、Excel 与公开构建验证脚本共同发布。

保留边界：40 条关系 hold、research_gap、政治诗歌 3/1/1、《消逝的足迹》年份 hold 和 author→event 说明级问题继续列入已知问题；发布不把 hold/gap 改写为已解决。网站、小程序、地图与展览交互仍属于 V2。

影响：`V1-N4-001` 与 `V1-S5-005` 完成；`v1.0.0` 标签和 GitHub Release 构成 V1 权威发布点。V1.0.0 之后的新关系与界面开发进入后续版本规划。

## DEC-040：V2 启动与 Charter 扩展

用户授权：USER 已批准《V2 网站产品决策与开发总说明书》，V2-N1 产品决策节点通过，并明确授权对冻结的 `PROJECT_CHARTER.md` 作一次必要的增量治理升级。

决定：V2 正式定位为基于 V1 正式研究数据库、面向中文读者的交互式拉丁美洲文学数字展览。V2 采用 Research Data + Curation Data → Web Data → Frontend 的分层，地图以作家地理、作品故事空间和文学虚构空间为主要类型，普通阅读层与研究证据层分离；详细产品设计仍以 V2 最高专项说明书为准。

范围：V2.0 先建立“发现文学 → 认识作家 → 进入作品 → 沿文学关系继续探索”的核心闭环。复杂知识图谱、用户系统、AI 问答、CMS、3D 地图、小程序、个性化推荐和多语言等不进入首版。

节点：纳入 V2-N1 产品决策批准、V2-N2 核心交互原型审核、V2-N3 完整测试站审核和 V2-N4 正式公开发布。本文该决定作出时，V2-N1 已完成，下一节点为 V2-N2；实时状态以 `V2_TASKS.md` 为准。

权限：节点之间 Codex 可自主完成普通开发、数据转换、构建脚本、自动 QA、技术选型、UI 细节优化、机械修复和有明确依据的策展内容；改变产品定位、地图主结构、研究证据标准、V2.0 范围或缺乏依据的重要文学策展判断，以及 N2—N4 正式节点，仍由 USER 决定。

章程影响：`PROJECT_CHARTER.md` 从 1.4.0 增量升级至 1.5.0。本次升级保留 V1 既有来源、数据、版权、公开仓库和 GitHub 治理规则，不写死具体技术框架，不将 V2 详细 UI 复制进总章程。`V2_TASKS.md` 为 V2 阶段唯一动态任务状态源。

## DEC-041：V2-N2 静态优先原型与 Web Data 契约

决定：V2-N2 采用 `site/` 静态单页原型，使用 `index.html + styles.css + app.js` 读取 `data/v2/web/site_data.json`；暂不引入运行时后端、外部地图瓦片或未经许可的图片资产。后续如更换 React/Next 等前端实现，必须继续消费同一 Web Data 契约，不得把研究事实迁移到组件代码。

数据边界：V2 的 Research Data、V2 地图技术补充和 Curation Data 由构建脚本生成 Web Data；公共策展层只接收 `auto_approved`，`user_review` 和 `hold` 保留在独立审核队列。虚构空间无现实坐标，历史事件只在页面和时间线中作为必要背景，不成为独立地图层。

影响：新增 `docs/V2_TECHNICAL_FOUNDATION.md`、`docs/V2_WEB_DATA_SCHEMA.md`、`docs/V2_N2_PROTOTYPE_REVIEW.md` 及 `site/` 原型；通过 `V2-N2` 前不解锁完整站点开发、N3 或 N4。本决定不改变 V1 主库、V1 Schema 或 V2 产品定位，动态执行状态继续以 `V2_TASKS.md` 为准。

## DEC-042：V2-N2 通过与阶段 5—7 解锁

用户授权：USER 于 2026-08-11 回复“可以，继续执行”，确认 N2 原型审核通过并授权继续。

决定：将 `V2-N2` 标记为 `✅ APPROVED`，解锁阶段 5 完整数据与策展接入、阶段 6 完整网站开发和阶段 7 完整测试站 QA。执行顺序仍为 `S5-001 → S5-002 → S5-003 → S6 → S7 → N3`，不得跳过页面覆盖、策展状态门禁或完整站 QA。

边界：本次授权不等于 V2-N3 或 V2-N4 通过。阶段 8 发布准备、正式部署、版本冻结和公开发布继续等待后续用户节点；不新增知识图谱、3D 地图、用户系统、AI 问答、CMS、多语言或个性化推荐。

影响：`V2_TASKS.md` 从 N2 `USER_REVIEW` 切换为阶段 5 执行；`V2-N3` 成为下一用户审核节点。本决定不改变 V1 数据、研究证据标准或 V2 地图三类空间范围。

## DEC-043：V2 完整测试站完成并提交 N3

决定：阶段 5—7 按 `S5-001 → S5-002 → S5-003 → S6 → S7` 顺序完成。完整测试站消费 V1 Research Data、V2 Geo Data 和分层 Curation Data 生成的 `v2-web-0.2` Web Data；首页、完整地图、国家/地点/作者/作品页、研究关联节点、搜索、时间线、证据层和响应式/可访问性均完成内部 QA。

门禁：公共策展只消费 `auto_approved`；research gap、hold、user_review、无卡关联节点和虚构空间无坐标规则继续保留。静态 HTTP 入口与 Web Data 均返回 200；Web Data QA、前端事实扫描、外部资产扫描、路由契约、语法检查和差异检查均通过。

节点：新增 `docs/V2_N3_FULL_TEST_SITE_REVIEW.md`，将 `V2-N3` 切换为 `👤 USER_REVIEW`。USER 需审核首页完整范围、地图语义、页面状态边界、搜索/时间线、研究层和手机端体验；N3 通过前不进入阶段 8 或 N4。

影响：本文该决定作出时，`V2_TASKS.md` 当前任务停在 `V2-N3`；本决定不授权正式发布、部署、版本冻结或公开 URL，不改变 V1 数据、V2 产品定位和三类地图空间范围。

## DEC-044：V2-N3 通过与 V2.0.0 发布候选准备

用户授权：USER 于 2026-08-11 回复“通过，继续执行”，确认完整测试站满足进入正式发布准备阶段的条件。

决定：解锁阶段 8，完成 V2-S8-001 至 V2-S8-004。V2.0.0-rc.1 固定使用 V1 主数据库、V2 地图数据、V2 策展数据、`v2-web-0.2` Web Data 和现有静态前端；候选包逐项记录 SHA-256 和构建统计。

公开边界：正式部署副本移除 `review_queue`，公共策展只保留 `auto_approved`；本地已有的 PDF、EPUB、原书、OCR、inputs 和其他受限材料继续排除。GitHub Pages 只准备手动触发工作流，并要求先配置已确认的 `V2_SITE_ORIGIN`。

节点：新增 `docs/V2_RELEASE_DATA_FREEZE.md`、`docs/V2_PUBLIC_BOUNDARY_QA.md`、`docs/V2_DEPLOYMENT_PREP.md`、`docs/V2_RELEASE_NOTES.md` 和 `.github/workflows/v2-pages.yml`。当前提交 `V2-N4 = USER_REVIEW`，未创建 V2.0.0 tag、未执行 Pages 部署、未发布正式 URL；这些动作等待 USER 最终批准。

影响：V2-N3 完成，阶段 8 发布准备完成；V2-N4 仍由 USER 决定，不改变 V2 产品定位、地图三类空间和研究证据标准。

## DEC-045：Web 0.1.0 重基线与 Public Release 暂停

用户授权：USER 于 2026-08-17 明确决定，V2 继续表示项目的网站建设阶段，但不再作为网站产品版本号；当前网站产品成熟度重新基线为 `Web 0.1.0`。USER 同时明确授权仅为落实本次版本治理调整，对冻结的 `PROJECT_CHARTER.md` 作一次必要、最小范围的增量修改。

决定：项目此后明确区分三套编号：`V1 / V2 / V3` 是项目阶段；Research Data 保留既有独立版本体系和当前主数据库/导出命名；Web Product 从 `Web 0.1.0` 开始按 0.x 语义演进。`V2.0.0-rc.1`—`V2.0.0-rc.5` 作为 Web 0.1.0 形成前的历史开发候选完整保留，不删除、不篡改、不重写。

当前状态：`WEB-0.1-BASELINE` 表示网站产品模型已经建立，PR #5 可作为后续内容扩张的开发基线合并；合并不代表正式上线、GitHub Release、production Pages deployment 或公开宣传。当前任务转为 `WEB-CONTENT-EXPANSION`，以 Research → Research Review → SQLite → Curation → Web Data → Frontend 驱动持续增长。

发布与审核边界：原 `V2-N4` 发布流程暂停，未来改由 USER 单独重新开启 `V2-PUBLIC-RELEASE` Gate。现有 `user_review` 与 `hold` 保持原状态，不要求 USER 为合并开发基线立即完成全部策展长文案审核；内部环境可以构建 USER_REVIEW preview，未来 Public Build 仍不得包含未批准策展内容。只有 USER 明确开启并批准 Public Release Gate 后，才可使用 `Web 1.0.0`、创建 GitHub Release 或执行 production deployment。

章程影响：`PROJECT_CHARTER.md` 从 1.5.0 增量升级至 1.6.0。本次只调整项目阶段、Research Data / Web Product 版本边界、V2 当前阶段与正式发布 Gate；不削弱 SQLite 单一主数据、Research / Curation 分离、来源与证据、版权、地图、USER 审核、GitHub 留痕、前端不得修改研究事实等任何冻结规则。
